# Bad-variant fixture: auth-flow-bad-yaml
# Defect: spec.json contains invalid JSON (unquoted key + missing closing brace)
# This fixture is consumed by spec lifecycle agents (spec-format-gate) — NOT by the compat runner.
# It documents the EXPECTED gate decision when the spec-format-gate processes this fixture.

fixture_type: bad-variant
expected_gate: SPEC_FORMAT_GATE_FAIL
expected_hardness: HARD
expected_decision: NO-GO
reason: "spec.json contains invalid JSON — unquoted key + missing brace; spec-format-gate critical-artifact rule forces NO-GO"
triggers_check: "spec.json valid JSON parsing"
