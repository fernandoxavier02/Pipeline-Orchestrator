{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_note": "DEV-ONLY project settings — NOT shipped (package.json files[] ships hooks/ + .claude/hooks/, never this file) and NOT the installed plugin's config. Enforcement-audit 2026-06-21: (1) removed the subagent_permissions/max_depth/max_parallel_agents block — Claude Code v2.1.185 ignores those keys (zero code refs); they were false enforcement. Leaf-spawn/parallel containment is code-enforced by .claude/hooks/dispatch-guard.cjs + parallel-dispatch-gate.cjs. (2) removed the duplicate Stop hook registration; stop-hook.cjs is registered exactly once, in the canonical shipped manifest hooks/hooks.json (alongside completion-checklist.cjs + session-cleanup-hook.cjs). A second registration here double-fired stop-hook.cjs in the dev/dogfood runtime.",
  "_armed_2026_06_21": "T13 SubagentStop commit ARMED (PIPELINE_SUBAGENTSTOP_ENFORCEMENT=deny) after a LIVE harness probe proved end-to-end: the harness fires SubagentStop with last_assistant_message, HONORS a decision:block (re-runs the blocked subagent with stop_hook_active=true), and our hook's 3-cap→hard_failed prevents deadlock. Green-close (PIPELINE_REQUIRE_GREEN_CLOSE) left OFF — the 4 pre-existing Langfuse env failures would make it block. Effect requires the v8.10.0 hooks to be loaded (via --plugin-dir here, or a published+updated install; the cached 7.13.0 plugin lacks them).",
  "env": {
    "PIPELINE_SUBAGENTSTOP_ENFORCEMENT": "deny"
  }
}
