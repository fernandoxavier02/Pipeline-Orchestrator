# Pipeline Orchestrator OpenCode Adaptation

This package contains the OpenCode-owned adaptation for Pipeline Orchestrator.
It is intentionally separate from the Claude Code plugin runtime surfaces.

Run the local verification suite with `npm test`.
