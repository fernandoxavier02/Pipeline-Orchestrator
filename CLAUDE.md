# CLAUDE.md — Pipeline-Orchestrator (canonical)

## Estilo de comunicacao (vale neste projeto tambem)

Texto fluido em paragrafos curtos, linguagem do dia a dia, veredicto na primeira linha (deu certo, deu errado, ou inconclusivo, e por que), detalhe vindo em seguida. O usuario nao e programador; escreva como se conversasse com alguem inteligente que nao programa. Termo tecnico so entra se vier com uma explicacao curta logo em seguida, na primeira vez que aparece. Sem isso, troque por palavra comum.

Este projeto tem um agravante importante: o usuario costuma escutar as respostas no modo de voz, entao **o texto precisa soar bem em voz alta**, nao so ler bem na tela. Isso impoe restricoes especificas:

A narrativa nunca carrega caminho de arquivo, endereco da internet, hash de commit, identificador de versao, tag entre colchetes, sigla nao explicada, ou nome tecnico de gate. Se a explicacao precisa de algum desses, ele vai num bloco de comando agrupado no FINAL da resposta, separado da prosa. O leitor de voz pula blocos de comando naturalmente; ja paths e enderecos lidos letra por letra destroem a experiencia. Em vez de dizer "edite o arquivo tal ponto md", diga "edite o agente de planejamento". Em vez de "veja o link da issue", diga "abri uma issue para registrar isso" e ponha o endereco la no bloco de comandos no fim, se for mesmo necessario o usuario abrir.

Bullets, tabelas e cabecalhos sao para comparacao real ou inventario; quando 2-3 frases corridas resolvem, prefira prosa. Listas com marcadores fragmentam a leitura em voz; paragrafos curtos fluem. Tabela so com 3 ou mais colunas e uma decisao por linha; do contrario, vira ruido.

Completude continua exigida. Cortar nome de arquivo nao e cortar a explicacao do que aconteceu, por que importou e o que fazer agora. A resposta tem que entregar essas tres coisas independente do canal.

Modo tecnico completo (com paths, hashes, listas de gates pelos nomes exatos, citacoes de linha) so quando o usuario pedir explicitamente, com expressoes do tipo "me detalha", "modo verboso", "me mostra os arquivos". Fora isso, mesmo discussao de arquitetura e pipeline vai na narrativa em portugues comum.

**Marcacao visual para o canal da tela.** A leitura por voz ignora marcacao de markdown — negrito, italico, codigo entre crases tudo passa em branco no audio. Isso libera essas marcacoes para destacar o que importa na leitura visual sem poluir o audio. Use negrito para o veredito final (**deu certo**, **deu errado**, **inconclusivo**), codigo entre crases para resultados numericos e estados que merecem destaque (`42 de 42 verde`, `build ok`, `EXIT=0`), e italico com moderacao para enfase. Use com parcimonia: duas ou tres marcacoes por resposta resolvem; markdown empilhado vira ruido visual igual a prosa empilhada.

**Brevidade agressiva.** Tamanho ideal da resposta principal: **dois a tres paragrafos curtos**, no maximo. Se sobrar coisa pra dizer, vai pra um bloco de comandos no fim ou pra um follow-up. Resposta longa e falha de filtro — significa que voce nao decidiu o que cortar. O usuario pede mais detalhe se quiser; o padrao e enxuto. Em particular: nao repita o que ja foi dito, nao expanda o veredito em sinonimos, nao explique o que voce vai fazer antes de fazer (so faca e reporte).

A regra mestra inquebravel com as seis regras detalhadas, exemplos de antes/depois e teste de ouvido vive no CLAUDE.md global do usuario, secao "Estilo de Conversa com o Usuario". Aquele documento e a fonte unica; este aqui apenas reforca, com a observacao adicional de que neste projeto o piloto opera por voz com frequencia, entao o teste do ouvido nao e cosmético, e gate de qualidade.

---

## Identidade

**Nome:** Pipeline-Orchestrator (CC plugin para Claude Code)
**Propósito:** caixa de vidro multi-agente — todo trabalho não-trivial passa por triagem, planejamento, execução em batches com TDD e adversarial review, fechando com Pa de Cal e audit trail.
**Persona dos contributors:** arquitetos B2B sêniores. Plugin é OSS e shippado via marketplace; usuários incluem outros engenheiros, não consumidores finais.

## Source-of-Truth Layer Map

| Camada | SSOT | Notas |
|---|---|---|
| **Design v5** | `designs/pipeline-orchestrator-v5-consolidated.md` | 1796 linhas, sucessor único dos 3 docs em `designs/legacy/` |
| **Design v5 Slice 0** | `designs/slice-0/INVENTORY.md` + 4 SPIKEs | Reality-check pós-sync 4.1.3 — INVENTORY atualizado em 2026-04-30 (Slices 0+0.5+1 marcados como concluídos) |
| **Versão canônica** | `.claude-plugin/plugin.json` `"version"` | Atualmente: **8.19.1** (2026-06-30, PATCH — arm-marker consume-on-close deadlock fix: o marcador `pipeline-arm-pending` não era consumido quando um run governado encerrava de propósito — só o TTL de 30 min ou um restart o limpava — então um run que mexe em código deixava o marcador vivo e o arm-gate seguia bloqueando a PRÓXIMA tarefa não-pipeline da mesma sessão com `PIPELINE_NOT_ARMED` (REVIEW-ONLY/DIAGNOSTIC nunca armam; o gatilho é um run FULL/código que armou, concluiu e nunca foi desarmado). FIX: `stop-gate-hook.cjs` consome o marcador nos caminhos de ALLOW de fechamento (completo/GO/spec-selado/hard_failed-cap/inativo-deliberado) via novo `lib/pipeline-arm.cjs::clearArmPendingForSession(cwd, sessionId)` — nunca num block, no recovery SessionEnd/StopFailure, ou em estado corrompido. Delete por POSSE-POSITIVA (só apaga quando os session_id de marcador e chamador estão ambos presentes e iguais, OU ambos ausentes; caso ambíguo/divergente é preservado pro TTL) + veredicto de adulteração delegado ao SSOT `lib/arm-pending` (marcador assinado-mas-inválido fica fail-closed). 2 rondas adversariais zero-context (1 HIGH tamper-bypass + 1 CRÍTICO cross-session, ambos corrigidos), passe final CLEAN. TDD `tests/regression/v8.19.1/F1` (10 cenários — os 5 call sites + tamper + tabela-verdade de posse). Iron Law: só `.claude/hooks/stop-gate-hook.cjs` + `lib/pipeline-arm.cjs` + `tests/`; Mandatory Gates table + Gate Registry INTOCADAS; sem espelho `.codex`. Suíte 250/5 (5 pré-existentes). Lockstep 9 superfícies.) Anterior: **8.19.0** (2026-06-29, MINOR — spec-authoring output enforcement: fecha o gap de comunicação/canal-de-retorno do spec-authoring (o seal já era code-enforced, mas o contrato de o spec-controller emitir um bloco terminal + o parent expô-lo era honor-only): (A) `spec-controller` Step 9 deve terminar num bloco cercado `PIPELINE_AGENT_RESULT_V1`; (B) `spec-controller` é folha Spec governada em `lib/contracts/workflow-manifest.cjs` resolvida por `spec_authoring_progress`, policiada pelo SubagentStop check já armado; comentário stale DEFAULT-OFF corrigido; (C) o Stop gate fecha um run Spec só com evidência de seal em disco, não por mera inatividade (HIGH-1). Emit-and-hoist safety: stops intermediários `AWAITING_*` passam un-counted; só o stop terminal é policiado. Seal-forgery guard: autoridade do seal consolidada no `final_decision` do sentinel assinado (o status do `manifest.yaml` não-assinado deixa de ser concessão independente) via `lib/contracts/workflow-manifest.cjs::sealEvidence`. Loop adversarial convergiu (1 CRÍTICO hoist-cap + 1 LOW over-match + 1 LOW manifest-forgery, todos corrigidos). Testes `tests/regression/v8.19.0/F1-F8` + contrato do manifesto. Iron Law: 2 hooks + `spec-controller.md` + contrato do manifesto + tests; Mandatory Gates table + Gate Registry INTOCADAS; sem espelho `.codex`. Lockstep 9 superfícies.) Anterior: **8.18.2** (2026-06-29, PATCH — observer governance guard: o `corrective-error-signal-gate.cjs` tratava QUALQUER subagente com `agent_type` como folha governada e emitia `SUBAGENTSTOP_RESULT_MISSING` + corretiva espúria para agentes do harness fora do pipeline (`Explore`/`general-purpose`), observado ao vivo em 2026-06-29 (run_id:""). Fix: a inversão agente→passo foi promovida ao SSOT `lib/contracts/workflow-manifest.cjs` como `stepForAgentType` + novo `isGovernedLeaf`, consumida pelo observador (novo guard membership-by-name) E pelo `subagent-stop-commit-hook.cjs` (delega ao manifesto, fim da duplicação). Observador deliberadamente membership-by-name/run-blind (documentado; NÃO idêntico ao commit-gate run-aware). +tests/regression/v8.18.2/F1 (8 casos, RED→GREEN do Explore) + contrato do manifesto S1-14..S1-17 (9 folhas + null-spine + falsy + trava de paridade manifesto↔step-ledger AGENT_STEP_MAP.FULL). Revisão adversarial 3 lentes zero-context em loop até CLEAN (segurança/arquitetura/qualidade): nenhum CRÍTICO/ALTO; achados de comentário enganoso ("identical posture") + cobertura/drift corrigidos. Iron Law: só `.claude/hooks/`, `lib/`, `tests/`; Mandatory Gates table + Gate Registry INTOCADAS. Suíte 241/5 (5 pré-existentes: 3 Langfuse + score-writer + F4 dispatch-pending-gate herdado da v8.18.0). Lockstep 9 superfícies.) Anterior: **8.18.1** (2026-06-28, MINOR — bugfix autônomo end-to-end de 4 achados da auditoria + 1 meta-escape autorizada pelo usuário: AUDIT-001 (`.claude/hooks/stop-hook.cjs` ganha `verifyRunLogAppend` chamado após `appendRunLog`; warn em mismatch `run-log verify mismatch`; soft-fail); AUDIT-004 (novo `lib/enforcement-status.cjs::detectEnforcementStatus` invocado pelo `enforcement-surface-verify-hook` SessionStart, emite evento dedicado `ENFORCEMENT_INACTIVE` HIGH; registrado em `lib/contracts/e2e-eval.cjs::HYGIENE_EVENT_SEVERITY` com peso 1.0); AUDIT-005 (`buildAuditCorrectiveDescriptor` ganha `corrective_action: "dispatch_agent"` + `agent_type: "audit-intake"` e o `buildAuditInlineReason` ganha sufixo `[corrective_action=dispatch_agent agent_type=audit-intake]`); AUDIT-006 (`scoreRun` curto-circuita runs abortadas — `state.abort_reason` + `pipeline_outcome` começando com `ABORTED_` → drop step/gate/order, único `hygiene_violation` PIPELINE_ABORTED HIGH); META (`PIPELINE_AUTONOMOUS_RECOVERY_MODE=true` em `skill-frontmatter-parser.cjs::getEnforcementMode` baixa `deny` para `warn` por processo — case-sensitive, downgrade-only, 1 linha de auditoria em stderr, escape sancionado pelo usuário para o deadlock chain-tied). TDD RED→GREEN via `tests/regression/v8.17.0/F1-F5` (5 arquivos, 35+ casos); suíte 4 fail = baseline pré-existente (F8/F9/F10 langfuse + F2 score-writer). Iron Law preservada: só `.claude/hooks/`, `.codex/hooks/`, `lib/` e `tests/` tocados; tabela Mandatory Gates 35 + Gate Registry 49 INTOCADAS. Lockstep 9 superfícies.) Anterior: **8.16.0** (2026-06-28, MINOR — bugfix autônomo dos 2 glitches que o avaliador E2E v8.15.0 pegou no run de auto-auditoria v8.15.1: Glitch A (`missing_step:final`) está PINADO pelo F1 (4 cenários) que protege o contrato existente do `step-ledger-stamp.cjs`; Glitch B (`BRAINSTORM_EVIDENCE_MISSING`) fechado adicionando ao mesmo hook um emit idempotente de `STEP_1_7_ROUTING(no-prep-override)` na primeira vez que um passo governado é carimbado num run de entrada direta — escopo MEDIA/COMPLEXA/Spec, pula quando o run já tem `step_1_7` no estado assinado ou uma entrada `STEP_1_7_ROUTING` no `gate-decisions.jsonl`. Ambos via o SSOT `lib/step-1-7-routing.cjs::appendStep17Routing`. Suíte 234/4 (baseline 232 + 2 arquivos novos; as 4 falhas Langfuse/env pré-existentes permanecem). Iron Law preservada: só `.claude/hooks/step-ledger-stamp.cjs` + `tests/` tocados; agents/skills/references/lib intocados; tabela Mandatory Gates + Gate Registry INTOCADAS — é evento de auditoria existente, não gate novo.) Anterior: **8.15.1** (2026-06-27, PATCH — conserto da persistência da isenção de despacho no orçamento de leitura do AUDIT: `writeLedger()` em `lib/audit-read-budget.cjs` passa a preservar o flag `dispatched` entre escritas, então o contador PostToolUse (`bumpLedger`) não apaga mais a isenção de `markDispatched` na primeira leitura — um subagente de audit/review despachado fica isento do teto de leitura inline pela janela governada inteira, em vez de ser re-bloqueado após uma leitura (o gate estava estrangulando os próprios revisores despachados, observado ao vivo). O flag é lido sob a MESMA guarda de tamper + `arm_id` que a contagem, então reset-por-invocação e reset-por-tamper continuam válidos. TDD RED→GREEN via `tests/regression/v8.14.0/F11` (bloco de regressão); revisão adversarial independente 0 crítico/0 alto/0 médio (1 info: forja unsigned `dispatched:true` persiste — postura cooperativa pré-existente, fechada por `PIPELINE_HMAC_STRICT`); suíte 232/4 (4 Langfuse/env pré-existentes). Iron Law: só `lib/` + `tests/`; Mandatory Gates + Registry INTOCADOS.) Anterior: **8.15.0** (2026-06-26, MINOR — remediação do enforcement chain-tied + loop de auto-avaliação E2E, num pacote só. (1) Enforcement: leitura do marcador arm-pending consolidada num SSOT (lib/arm-pending.cjs) consumido pelo arm-gate, pelo audit-read-budget-gate e pelo novo hook audit-dispatch-marker.cjs (fecha violação DRY/DIP onde a mesma lógica verify-on-read + TTL vivia em 3 lugares e dois hooks pegavam predicados emprestados de um hook irmão); o orçamento de leitura do AUDIT passa a ISENTAR um subagente genuinamente despachado, pra orquestração legítima nunca ser estrangulada pelo teto. 3 achados da revisão final de segurança corrigidos: SEC-1 a marca de despacho usa ALLOWLIST (só agentes reais de review/audit/ux marcam o ledger; sentinel/task-orchestrator/passos de brainstorm nunca — a denylist anterior colidia); SEC-3 a mensagem de negação de rehidratação não ensina mais o caminho de escrita da exec-window ("a janela não se abre à mão"); SEC-4 o rótulo de workflow ecoado numa razão de negação vista pelo LLM é reconstruído de uma allowlist MODE/Type validada (SSOT anti-injeção em lib/pipeline-workflow-classifier.cjs). (2) Loop de auto-avaliação E2E: avaliador determinístico passo-a-passo (lib/e2e-evaluator.cjs) sobre um contrato explícito (lib/contracts/e2e-eval.cjs), hook e2e-eval-gate.cjs, loop-closer scripts/e2e-eval-complete.cjs e comando /self-eval pontuam um run terminado e levantam propostas de melhoria HUMANO-GATED no Stop (≈70% da infra de fidelidade/log reusada). LIMITAÇÃO CONHECIDA (piso do modelo cooperativo): o enforcement é por prompt/coordenação, NÃO criptográfico contra o parent — um parent com escrita em .pipeline/ ainda pode se desgovernar apagando o próprio estado de coordenação (equivale a nunca invocar o pipeline). Fechar essa classe inteira exige PIPELINE_HMAC_STRICT default-on no plugin todo — mudança de postura plugin-wide, adiada de propósito pra um major futuro (mexer nela agora atingiria muitos arquivos fora deste changeset e quebraria o default unsigned tolerante-a-migração que o resto do plugin usa). Testes tests/regression/v8.14.0/ (F1-F13, incl. F12 paridade SSOT gate↔lib e F13 injeção SEC-4) + tests/regression/v8.15.0/; suíte 232/4 (as 4 são as falhas pré-existentes Langfuse/env: F8/F9/F10 langfuse + F2 score-writer). Iron Law preservada: só .claude/hooks/ + lib/ + hooks.json + commands/ + scripts/ + tests/ — tabela Mandatory Gates by Complexity e Gate Registry INTOCADAS. Lockstep 9 superfícies.) Anterior: **8.13.0** (2026-06-23, MINOR — fecha o furo do AUDIT inline (read-only escape) com um novo gate determinístico bloqueador. O pipeline-arm-gate sempre libera Read/Grep/Glob (pra não congelar diagnóstico), então tarefas read-only (Audit, UX Simulation) rodavam inteiras INLINE sem armar o run nem despachar os agentes — orquestração silenciosamente pulada (mesma classe do R1-SEC-2 da v8.12.1: enforcement preso a eventos de mutação, cego à leitura). FIX: na janela governada-não-armada, conta as leituras de investigação num ledger ASSINADO (.pipeline/state/governed-read-ledger.json, keyed por sha256(requested_at), reset por invocação) e BLOQUEIA a (teto+1)ª leitura para os tipos read-only escapáveis (Audit, UX Simulation); teto default 12 (env PIPELINE_AUDIT_READ_BUDGET, floor 1), deny por padrão (único escape PIPELINE_AUDIT_BUDGET_ENFORCEMENT=warn). Dois hooks — PreToolUse blocker (.claude/hooks/audit-read-budget-gate.cjs) + PostToolUse counter (.claude/hooks/audit-read-counter-hook.cjs) — sobre um cérebro puro (lib/audit-read-budget.cjs, decideAuditReadBudget), reusando os predicados EXPORTADOS do pipeline-arm-gate (runStateContext/isPipelineDocWrite/markerIsTampered/resolveArmTtlMs) + o signer (verify two-tier) + exclusive-lock — ZERO mudança no arm-gate. Diagnóstico leve segue liberado; arming/dispatch e leituras de .pipeline/ são isentos; o marcador expira por TTL (sem deadlock). TDD RED→GREEN (B0 brain 15 + B0 contract 14 + B2 live 12 = 41 cenários) + revisão adversarial zero-context CLEAN (0 CRÍTICO/0 ALTO). Suíte 215/4 (4 Langfuse/env). Iron Law: só .claude/hooks/ + lib/ + hooks.json + tests/; registry 49 + Mandatory 24 INTOCADOS (é hook de enforcement, como dispatch-pending-gate — NÃO linha de registry). Backlog (MEDIUM/LOW aceitos, mesmo threat model da v8.12.1): ledger unsigned-tolerado (PIPELINE_HMAC_STRICT fecha); race no read sem lock (teto é heurístico, overshoot 1-2); isAlignedRead não checa Glob pattern (over-count, mais rígido); sanitizeWorkflow mantém / (paridade com o arm-gate).) Anterior: **8.12.1** (2026-06-23, PATCH — fecha as 2 HIGH de segurança da spec 006 que deixaram a v8.12.0 em CONDITIONAL, via pipeline bugfix-heavy completo (TDD RED→GREEN + revisão adversarial por batch + security scan final). R1-SEC-1: verify-on-read adicionado aos TRÊS canais corretivos (readActiveSentinelState, readCorrectiveTriggers, readErrorSignals) em lib/corrective-dispatch.cjs — polaridade two-tier (assinatura presente+inválida REJEITA/descarta fail-closed; unsigned TOLERA migração; chave ausente TOLERA), reusando o signer verifyState, espelhando edit-guard-hook. R1-SEC-2: matcher PostToolUse do corrective-dispatch-gate (hooks/hooks.json) ampliado para incluir Read|Glob|Grep, religando o dreno na janela governada-não-armada (o gate lê via fs, sem loop; teto M1 como backstop). +tests/regression/v8.12.0/SEC_2high_fix.cjs (7 cenários RED→GREEN). Batch A/B reviews CLEAN; security scan final 0 CRIT/0 HIGH (as 2 HIGH GENUINAMENTE fechadas). Pa de Cal GO @ confidence 1.0. Suíte 212/4 (4 Langfuse/env). Iron Law: só lib/corrective-dispatch.cjs + hooks/hooks.json + tests/ (registry 49 + Mandatory 24 INTOCADOS). Backlog não-bloqueante (MEDIUM/LOW pré-existentes): documentar strip-attack + PIPELINE_HMAC_STRICT, integridade do ponteiro active-run.json, assinar/documentar control-files (ledger/consumed).) Anterior: **8.12.0** (2026-06-23, MINOR — spec 006 self-healing remediation, Batches 4-7: motor corretivo LAYER A→B (lib/corrective-dispatch.cjs) ligado via hook determinístico (corrective-dispatch-gate) + LAYER B canal error-signals + corretor (corrective-error-signal-gate) + auto-verificação de superfície na largada (enforcement-surface-verify-hook) + arm de sessão (pipeline-session-arm-hook). TDD RED→GREEN + revisão adversarial por batch (1 CRITICAL no B6 e 1 HIGH no B5 corrigidos no ciclo). Suíte 211/4 (4 Langfuse/env pré-existentes). Iron Law preservada (registry 49 + Mandatory 24 INTOCADOS). KNOWN ISSUES (2 HIGH, correção em andamento via run de follow-up): R1-SEC-1 (canais corretivos assinam na escrita mas NÃO verificam na leitura → estado/gatilho on-disk forjável); R1-SEC-2 (corrective-dispatch-gate registrado em PostToolUse Edit|Write|Bash que é negado no PreToolUse na janela governada → dreno inativo em prod). Veredicto final-validator: CONDITIONAL. NÃO commitado como GO — release de checkpoint a pedido do usuário, com as 2 HIGH documentadas e fix em sequência.) Anterior: **8.11.1** (2026-06-22, PATCH — enforcement deadlock fix [dogfood]: o dispatch-pending-gate passa a ISENTAR o spawn do agente sentinel nos dois ramos (handshake pendente vivo + estado assinado CORRUPT), espelhando a isenção que o sentinel-hook já dava ("sentinel always passes"). Fecha o deadlock irrecuperável em que uma divergência de sequência manda o parent rodar o sentinel para auto-corrigir, mas a trava de passo-pendente barra o spawn do sentinel — deixando o run preso. O sentinel é read-mostly (recuperação/diagnóstico), não trabalho de produção inline, logo não é o wrong-Agent bypass. +tests/regression/v8.12.0/T4_sentinel_recovery_exemption.cjs (6 cenários RED→GREEN, incl. segurança: agente não-sentinel + Read inline seguem bloqueados; F37 intacto). Iron Law: só .claude/hooks/ + tests/; sem espelho .codex (dispatch-pending-gate não espelha); suíte 206/4 (4 Langfuse/env pré-existentes). NÃO altera comportamento de usuário.) Anterior: **8.11.0** (2026-06-22, MINOR — default-ON enforcement arming + the 5 missing wires [docs/audits/2026-06-22-enforcement-arming/]: the two strong gates now ship ARMED BY DEFAULT in shipped code — SubagentStop commit (B4, subagent-stop-commit-hook default deny, escape `PIPELINE_SUBAGENTSTOP_ENFORCEMENT=warn`) + incomplete-run Stop block (B5, stop-gate-hook isArmed default-on, escape `PIPELINE_STOP_BLOCK_ENFORCEMENT=warn`) — so a shipped install enforces them (the prior arming lived in dev-only settings.json, never packaged). NEW WIRES: B1 happy-path completer — stop-gate persists `terminal_state=completed` from a real GO/CLOSEOUT_CONFIRM/PA_DE_CAL signal (signed-state primary + bounded gate-decisions tail), so arming the Stop block cannot deadlock a finished run; commit-flag persist re-checks terminal under lock (lost-update + idempotency safe). B2 green-close producer WIRED — machine-evidence-hook calls produceCheckpointVerdict ONLY on test/checkpoint commands (runner-aware filter, env/sudo/time prefixes tolerated), recognizes pytest/jest/go summaries, feeds `last_checkpoint_verdict` with {countFailure:false} so the A2/A3 counter is never double-counted (a pass still resets it). B3 consume-once — step-ledger-stamp renames a step-unlocking verdict file (`…consumed-N`) after a successful record so a stale verdict can't re-unlock a new batch (collision-free, audit-preserving; batch-files domains left accumulating by design). B4 readiness test locks that all 9 governed agents emit PIPELINE_AGENT_RESULT_V1. B5 fail-safe — an unresolvable state path ALLOWS rather than infinite-block. Per-batch adversarial review (3 zero-context lenses) + final 3-lens integration review converged: real defects fixed (overwriting a concurrent aborted, build-intermediate false-fail, double-count, reset-on-pass, null-statePath infinite deadlock), false alarms (HMAC of gate-decisions, race-with-cleanup) refuted with code evidence (CORRUPT_SENTINEL guard + H3 test). Live `--plugin-dir` probe (OAuth, Claude Code 2.1.183): code loads/validates/runs, enforcement hooks fire live and write canonical gate decisions, a control run (armed vs disarmed) was IDENTICAL → the new gates are flow-neutral; full end-to-end close needs an interactive session (the pipeline has human gates that headless `-p` cannot answer). Iron Law preserved: only `.claude/hooks/`,`lib/`,`scripts/`,`tests/`; Mandatory Gates table + Gate Registry UNTOUCHED; no production agent `.md` changed (only test co-updates T13/T14/F5/F6/F7). tests/regression/v8.11.0/ B1-B5 green; full suite only the pre-existing Langfuse/vendoring env failures + F07 flaky. Lockstep 9 surfaces. NOT committed/published — user decides.) Anterior: **8.10.0** (2026-06-21, MINOR — deferred-enforcement layer built against the LIVE-probe-confirmed Claude Code hook contract: SubagentStop commit point (reads the agent’s real last_assistant_message, BLOCKS on missing/invalid result, 3-cap→hard_failed, DEFAULT-OFF), 9 governed agents emit PIPELINE_AGENT_RESULT_V1, dispatch updatedInput envelope (prepend-once), PostToolUse machine-evidence collector (reads tool_response + interrupted — live finding: the harness exposes NO Bash exit code), Stop additionalContext recovery signal, leaf-spawn lock (Agent only on the 6 genuine orchestrators), green-close checkpoint-verdict producer (closes the v8.9.0 C1 hole; derives the verdict from the runner stdout summary). Full-coverage spec (18 phases + PLAN MODE + 22 DoD, zero GAP; FASE 9 + FASE 14 that the prior run silently skipped are now dispositioned). A live --plugin-dir probe proved the harness HONORS a SubagentStop decision:block (re-runs the blocked subagent with stop_hook_active); T13 ARMED via PIPELINE_SUBAGENTSTOP_ENFORCEMENT=deny. Per-batch + final adversarial review caught real bugs both runs (signature fail-open, unregistered StopFailure, leaf-pin over-reach on 3 review orchestrators, ephemeral-key deadlock, tool_response field). Iron Law preserved. Lockstep 8 surfaces.) Anterior: **8.9.0** (2026-06-20, MINOR — flow-determinism gates Onda 1+2 [auditoria docs/audits/2026-06-19-enforcement-determinism/FLOW-DETERMINISM-AUDIT.md]: converte pontos de fluxo antes prompt-only em gates determinísticos que NEGAM. Onda 1: `lib/checkpoint-verdict.cjs` (A1 não-avançar-no-build-vermelho) + `lib/consecutive-failure-counter.cjs` (A2/A3 STOP_RULE em 2 falhas) + `lib/domain-scanner.cjs` (A4 domínio sensível auth/cripto/pagamento/dados → revisão adversarial OBRIGATÓRIA, sem escape warn). Onda 2: `lib/phase-verdict-guard.cjs` genérico (A5-A9: SSOT conflito, info-gate bloqueado, plano rejeitado, crítico final aberto, NO-GO bloqueiam o agente certo). Novos hooks `checkpoint-verdict-gate.cjs` + `phase-verdict-gate.cjs` (CORRUPT fail-closed pra leaf governado, default deny, escape warn); `recordCheckpointVerdict`/`recordDomainsTouched`/`recordPhaseVerdict` no `record-step.cjs` (allowlist anti-injeção, sob exclusive-lock); carimbos no `step-ledger-stamp.cjs` via arquivos de veredito. LADO CONSUMIDOR: os gates fazem fail-OPEN (allow) quando o agente produtor ainda não emitiu o veredito — ativação progressiva conforme produtores forem ligados; a regra fechar-com-verde (A1b) é OPT-IN via `PIPELINE_REQUIRE_GREEN_CLOSE` pra nunca dar deadlock no fluxo padrão. Revisão adversarial per-onda + final (3 revisores zero-context): 1 HIGH (produtor não-ligado → A1b opt-in, deadlock removido), plural-evasion no scanner corrigido (`sessions/`,`payments/`), overwrite-é-caminho-corretivo documentado. Testes `tests/regression/v8.9.0/` F1-F3 (39 verde); suíte 176/180 (4 Langfuse pré-existentes de env). Iron Law preservada — só `hooks/`,`lib/`,`scripts/`,`tests/`; tabela Mandatory Gates + Registry INTOCADAS; sem TypeScript. Lockstep 9 superfícies.) Anterior: **8.8.0** (2026-06-20, MINOR — determinism enforcement hardening [auditoria flow-determinism 2026-06-19 AUDIT-001..007]: B1 gate-log-gate default warn→deny para leaves governados; B2 step-ledger-gate + gate-log-gate + dispatch-pending-gate falham CLOSED em sentinel-state corrompido para ações GOVERNADAS (paridade com batch-review-gate; ausente fica fail-open, warn-escape relaxa, ungoverned nunca trava); B3 novo enforcement write-time no scope-lock-hook — a PRIMEIRA escrita de produção sob run refactor-light/heavy é negada até REFACTOR_SCOPE_LOCK constar em gate-decisions.jsonl (escape PIPELINE_REFACTOR_SCOPE_LOCK_ENFORCEMENT=warn; .pipeline/ isento; governed-fail-closed); B4 nova skill de roteamento bare skills/refactor/SKILL.md (espelha bugfix, sem steps/); B5 lib/pipeline-arm.cjs assina o marcador arm-pending via o signer HMAC do sentinel-state (SSOT) e o pipeline-arm-gate verifica (marcador adulterado = não-armado; unsigned tolerado salvo PIPELINE_HMAC_STRICT). Iron Law preservada: tabela Mandatory Gates by Complexity e Gate Registry de references/gates.md INTOCADAS — todo comportamento vive nos hooks existentes + skill aditiva; só .cjs+.md, sem TypeScript. Testes tests/regression/v8.8.0/ F3-F9 TDD RED→GREEN. Lockstep 9 superfícies.) Anterior: **8.7.0** (2026-06-18, MINOR — deterministic enforcement layer: novos hooks code-enforced (arm-gate, step-ledger, loop-cap, gate-log) que tornam o agente incapaz de fazer trabalho substantivo até um run de pipeline ser armado e rodado em ordem — fecha o furo em que qualquer `/pipeline` podia ser tratado inline sem enforcement porque tudo dependia de um sentinel-state que o LLM tinha que criar voluntariamente. Novos `lib/pipeline-workflow-classifier.cjs` + `lib/pipeline-arm.cjs` + `lib/step-ledger.cjs` + `lib/fix-loop.cjs` + `lib/gate-log-guard.cjs` e hooks UserPromptSubmit/PreToolUse/PostToolUse correspondentes; HMAC verificado antes de confiar/re-assinar (sem laundering de estado forjado); fix-loop cap determinístico (default 3); gate-log default warn (rollout). 12 suítes de regressão novas (tests/regression/v8.7.0/ F1–F12); 2 rounds adversariais com 6 revisores zero-context + 3 passes de fix fecharam 2 CRITICAL + 6 HIGH. Lockstep 9 superfícies.) Anterior: **8.6.0** (2026-06-18, MINOR — adversarial loop-breaker: novo gate ADVERSARIAL_LOOP_BREAKER (CIRCUIT_BREAKER) que substitui o "para no 3º" por um modelo de dois níveis — contador interno de 3 tentativas por ciclo dentro de um contador externo de 4 ciclos por batch; ao esgotar o 4º ciclo dispara o escape DIAGNOSE-THEN-REIMPLEMENT (review-orchestrator lê os relatórios adversariais anteriores, classifica achados-contraditórios/falso-positivo/restrição-impossível, e despacha executor-fix em modo REIMPLEMENT que reescreve do zero em vez de remendar); escape resolve → volta ao fluxo, escape falha ou já usado → FIX_LOOP_EXHAUSTED. Escopo universal: SIMPLES agora roda plan mode e adversarial review obrigatórios — conjunto mandatório SIMPLES 6→11, MEDIA 11→13, COMPLEXA 17→18; gate registry header 43→44; tabela Mandatory 23→24 linhas. Estado em sentinel-state.review_state ganha adversarial_loop_count + escape_used (reset por batch). Regra cumulativa preservada: FIX_LOOP_EXHAUSTED é mandatório para SIMPLES, logo também para MEDIA e COMPLEXA. Branched off 8.4.0; precisa rebase sobre a 8.5.0 in-flight de main. Nova suíte tests/regression/v8.6.0/F1 (17 testes verde); F7 e fixture phase-1-5-trigger atualizados; sem espelho .codex (só langfuse/stop espelham). Lockstep 9 superfícies. A mudança da Iron Law F1/tabela Mandatory é DELIBERADA aqui.) Anterior: **8.5.0** (2026-06-18, MINOR — deterministic /spec authoring enforcement force-back P1-P5: seal-gate (`sealSpecRun()` 4 preconditions + `spec-seal-guard.cjs`), `record-spec-step.cjs` per-step stamping, review-convergence check, code-produced `implementation_contract` + sealed-spec→implementation handoff guard (`dispatch-guard` `handleSealedSpecImplementation`, `final-validator` SPEC_CONTRACT_DISCIPLINE_MISSING, pipeline-controller Phase 0), structural parallel dispatch (DISPATCH_REQUEST `group_id`/`batch_mode` + `parallel-dispatch-gate.cjs`). 4 AUDIT gates (SPEC_AUTHORING_INCOMPLETE, SPEC_AUTHORING_STEP_BYPASS, SPEC_CONTRACT_DISCIPLINE_MISSING, PARALLEL_DISPATCH_VIOLATION) registrados em protocol-events.jsonl; gate registry header 43→47; Iron Law F1 Mandatory Gates table INTOCADA (23 rows). 2 novos hooks (`spec-seal-guard.cjs` Bash|PowerShell, `parallel-dispatch-gate.cjs` Agent) — sem espelho `.codex` por design (só langfuse/stop espelham). +tests/regression/v8.5.0/ F1-F6. Lockstep 9 superfícies.) Anterior: **8.4.0** (2026-06-18, MINOR — spec-workflow telemetry/fidelity bridge [auditoria audit-heavy AUDIT-001..006]: o workflow `/pipeline-orchestrator:spec` rodava em `pipeline-runs/` mas NÃO deixava rastro mensurável (run-log/Langfuse/fidelity zerados; REST confirmou 50 traces 0 spec). Correção ADITIVA (sem mover runs, sem tocar o scan `.pipeline/docs/` nem a Mandatory Gates table): `allocate()` publica `.pipeline/active-run.json` (gravação atômica) + `sentinel-state.json` assinado no run dir; `stop-hook`/`langfuse-hook` (+ espelhos `.codex`) descobrem um run SELADO em `pipeline-runs/` via o ponteiro mesmo inativo; novo `lib/run-seal.cjs` `sealSpecRun()` (manifest novo status `sealed` + sentinel fechado + `SPEC_SEALED` em gate-decisions, com CLI entry); `fidelity-reporter.cjs` régua sensível ao fluxo (authoring={SPEC_SEALED,SPEC_REVIEW_FINDINGS} via allowlist EXATA {spec-authoring,spec-author}; processing intocado) só honrada se a assinatura HMAC do sentinel conferir (estado forjado cai no conjunto seguro); `spec-controller.md` Step 0/8/9 amarrado à selagem. 3 rounds de revisão adversarial zero-context convergidos SEM CRITICAL/HIGH (pegaram o bug crítico de a variante não ser repassada no caminho real do teardown + downgrade por variante não-verificada + 8 itens menores, todos corrigidos). +`tests/regression/v8.4.0/F6` (17 testes) + F3 silent-skip removido; suíte 142/4 (4 Langfuse env pré-existentes); espelhos `.codex` byte-idênticos. Lockstep 9 superfícies.) Anterior: **8.3.0** (2026-06-17, MINOR — obrigar o dispatch de subagentes [auditoria AUDIT-001]: o enforcement de Plan Mode/Brainstorm vira default `deny` (era `warn`) no `dispatch-guard.cjs`; novo hook `dispatch-pending-gate.cjs` bloqueia QUALQUER ação de trabalho do parent (Read/Edit/Bash/Grep/MCP/…) enquanto há handshake pendente+vivo no sentinel-state, liberando só a RESOLUÇÃO (Agent do alvo OU com payload DISPATCH_RESULTS/GATE_RESPONSES/PLAN_MODE_RESULTS / AskUserQuestion / EnterPlanMode / Task*) — fecha o "trabalho inline sem escrever" que o parent usava pra burlar. Isenções anti-deadlock: exec-window, `.pipeline/`, timeout-recovery, estado ausente/corrupto=fail-open; escape `PIPELINE_DISPATCH_INLINE_ENFORCEMENT=warn`. Revisão adversarial 3 revisores zero-context: SEC-1 (wrong-Agent bypass) + SEC-3 (MCP via matcher catch-all) corrigidos. +F36/F37 (22 testes); hooks 185/185; suíte só 4 Langfuse + F07. `getActiveLock` exportado. Lockstep 9 superfícies.) Anterior: **8.2.2** (2026-06-17, PATCH — detectShellWrite back-port [auditoria A1]: porta pro canônico o conserto do detector de escrita-no-terminal já entregue no OpenCode v0.2.0 — redirect com descritor numerado (`2> arquivo`) agora detectado (`[^&]`), WRITE_CMDS expandido (touch/mkdir/rm/curl -o/tar -x/git checkout|apply|restore/python -m/Remove-Item), verbos avaliados na string sem aspas, heredoc no comando cru. Fecha o furo de escrita-via-terminal no produto principal com a trava armada. +5 testes `detect-shell-write-backport`; hooks 173/173; suíte só 4 Langfuse + F07. Achado pela auditoria empresarial do ecossistema 2026-06-17.) Anterior: **8.2.1** (2026-06-17, PATCH — Plan Mode deadlock fix: a trava de plano determinística v8.2.0 isentava apenas `.pipeline/`, então bloqueava a escrita do PRÓPRIO plano do Plan Mode (em `~/.claude/plans/` ou `$CLAUDE_CONFIG_DIR/plans`), criando deadlock em qualquer pipeline MEDIA/COMPLEXA/Spec que arma o gate na Fase 0 e tenta escrever o plano na Fase 1.5. Conserto: novas `isPlanFile()`/`isExemptPath()` em `.claude/hooks/edit-guard-hook.cjs` isentam o diretório de planos do harness nas 3 funções de bloqueio (`shouldBlockWithoutApprovedPlan`, `shouldBlockOnPendingDispatch`, `corruptResult`); código de produção fora dessas pastas continua barrado. Descoberto pela prova-viva (dogfood) durante a portabilidade da trava para o OpenCode. +5 testes `plan-mode-exemption.test.cjs` (RED→GREEN); suíte de hooks 168/168; demais travas intocadas; lockstep 9 superfícies.) Anterior: **8.2.0** (2026-06-17, PATCH — guard de doc do protocolo: nota anti-padrão explícita em `references/gate-request-protocol.md` (handler do parent) e `skills/spec/SKILL.md` (handler do Achado #7) — re-dispatch é uma chamada `Agent()` nova que re-hidrata o estado do run-dir, NÃO continuar um agente vivo; `SendMessage` não é usado por este pipeline e, mesmo onde existe, é deferred tool carregável via `ToolSearch`. Fecha o modo de falha em que o parent main LLM lê um handshake `AWAITING_*` como se precisasse de `SendMessage` e abandona o run. ZERO mudança de runtime.) Anterior: **8.0.2** (2026-06-16, PATCH — release hygiene: tarball npm limpo (remove o `.lock` de sessão de runtime vazado + commita o comando `/help` que só ia pro npm; `.npmignore` endurecido para `**/.pipeline/` e `*.lock`; novo teste `tests/packaging/no-pack-cruft`), os 13 testes do flow-mirror Paperclip agora entram no gate de release (categoria `paperclip` em `run-tests.cjs`), e a prosa de descrição/README defasada foi atualizada; ZERO mudança de runtime do plugin. Anterior: **8.0.1** (2026-06-15, PATCH — Paperclip layer reflete a v8.0.0 spec-authoring + conserto do handoff do flow-mirror (montagem progressiva + 8 causas-raiz estruturais + integridade de molde), vocabulário canônico de telemetria, roster reconciliado em 50; suíte spec/lib 13/13 (353 testes) verde; ZERO mudança no runtime do plugin/agents — additivo em references/paperclip. Anterior: **8.0.0** (2026-06-15, MAJOR — spec-authoring workflow take-over: `/pipeline-orchestrator:spec` passa a CRIAR uma especificação do zero (clarificação + ideação proativa + interrogador forçado + lifecycle Kiro + revisão adversarial dos documentos + selo de contrato) e para antes de implementar; processamento legado movido para `/spec-light`/`-heavy`/`-audit-only` e `/pipeline --type=Spec`. Novos: `agents/core/spec-controller`, `agents/brainstorm/step-01c-ideation`, `agents/executor/type-specific/spec-adversarial-critic`, `skills/spec-review`, fluxo `--amend`, `references/run-orchestration-substrate`. 8 eventos de telemetria novos (Gate Registry 35→43; tabela Mandatory Gates by Complexity INTOCADA, F1 preservado). autoDiscover cobre as superfícies novas. Iron Law preservada. Anterior: **7.14.1** (2026-06-14, PATCH — gate-decision SSOT shipped / schema-drift fix: entrega a Fase 1 do contract-enforcement que já estava em `main` (commit 4420907). Novo `lib/contracts/gate-decision.cjs` como fonte única do vocabulário canônico (8 decisões + 5 hardness + 13 chaves) consumido por `gate-decision-writer` + `jsonl-sanitizer` + `fidelity-reporter`; encerra o drift em que `final-validator`/`pipeline-controller`/`audit-trail` exigiam "exactly these 8 keys" e procuravam um `RESOLVED` que o writer nunca emite — fazendo o plugin reprovar o próprio audit trail de 13 campos como anômalo. Prosa alinhada ao writer real; novo teste `F-gate-decision-contract` (10/10). Auditoria audit-heavy do próprio pipeline (2026-06-14, `docs/audits/2026-06-14-typescript-contract-enforcement/`) confirmou base CORRETA (v7.14.0, git parent = release b6d383e), integração limpa e ZERO regressão (suíte 114/4/118; 4 falhas pré-existentes Langfuse+flaky). Iron Law: quebra mínima em `agents/`/`lib/`/`commands/`/`references/` reconhecida e justificada pelo bug de auto-auditoria; SCHEMA_VERSION intocado em '1'. Lockstep 9 superfícies F7 + cursor manifest.) Anterior: **7.14.0** (2026-06-12, MINOR — STEP 1.7 brainstorm enforcement determinístico: fecha o achado crítico da auditoria de classificação de 2026-06-12 (4 eventos `STEP_1_7_ROUTING` em 245 runs, enforcement prompt-only — mesmo padrão do PLAN_MODE_BYPASS pré-v7.9.4), no molde estado-verificável + trava warn-first + cobrança no fechamento. **B1:** novo `lib/step-1-7-routing.cjs` — vocabulário fechado de 4 branches congelado (`load-existing|dispatch-brainstorm|no-prep-override|simples-bypass`), mapeamento D1 para os 8 valores canônicos (branch no `detail`, vocabulário do writer INTOCADO), `appendStep17Routing` delegando ao SSOT writer com sanitização (allowlist em `prep_run_id`, `phase`/`decided_by` capados), `buildStep17StateBlock` (sentinel-state.step_1_7 guarda o branch CRU — consumidores validam contra BRANCH_VALUES) e `recordNoPrepOverride` (trilha auditada `.pipeline/state/no-prep-overrides.jsonl` com campo `prompt` forense, containment de path, mkdir, lock exclusivo); `codex-operational-runtime` delega ao helper + containment do `pipelineDocPath` caller-supplied + `brainstormConcluded` init `false` incondicional. **B2 (coração):** `dispatch-guard.cjs` ganha `handleBrainstormDispatch` espelhando o detector PLAN_MODE — spawn de alvo de Phase 2 (roster `references/step-1-7-enforcement.json` + fallback embutido FAIL-CLOSED com guard anti-cache-poisoning `targets.length>0` e `skill_prefixes` filtrado 1..32) com `orchestrator_decision` MEDIA/COMPLEXA ou type Spec E `step_1_7` ausente/inválido → evento AUDIT `BRAINSTORM_BYPASS` em protocol-events.jsonl + warn; `PIPELINE_BRAINSTORM_ENFORCEMENT=deny` bloqueia com mensagem corretiva (D2). Warn fail-OPEN com estado ilegível; deny fail-CLOSED. Dedup por leaf+run, namespace guard nos paths Agent E Skill, caps de tamanho, `sanitizeForReason` (printable-ASCII) no deny payload, verificação HMAC two-tier via sentinel-state-signer (unsigned tolerado em warn; assinatura inválida → `BRAINSTORM_STATE_UNSIGNED` + bloco tratado como ausente; `PIPELINE_HMAC_STRICT`+deny fail-closed); `planModeAppendEvent`→`appendProtocolEvent` compartilhado. **B3:** `final-validator.md` Step 1b-brainstorm (run qualificante sem `STEP_1_7_ROUTING` → evento AUDIT `BRAINSTORM_EVIDENCE_MISSING`, deliberadamente SEM downgrade de verdict) + `fidelity-reporter.cjs` warning em `warnings[]` (score binário intocado); prosa do controller D3 state-write-before-spawn + schema real do no-prep-overrides. **Testes:** `tests/regression/v7.14.0/` F33 (13 cenários comportamentais child-process) + F34 (7, pinado no export vivo) + F35 (6, caminhos legais), TDD RED→GREEN; 27 achados adversariais fechados (6 revisões per-batch zero-context em 5 fix loops + 3 revisores finais com 1 rework autorizado). **Dogfood vivo:** o próprio run de implementação provou os DOIS caminhos — fluxo correto logou `dispatch-brainstorm` na 1ª tentativa na Phase 0, e o bypass deliberado gerou `BRAINSTORM_BYPASS` warn + deny sob enforcement. Pa de Cal GO @ 0.85. NÃO há gate novo (Iron Law: registry 35 + Mandatory table + Inline Invariants intocados; BRAINSTORM_* são eventos AUDIT). Sem espelho `.codex` do dispatch-guard (não existe lá; paridade Codex via codex-operational-runtime). Backlog v7.15: require dinâmico no fallback de branches, extensibilidade do handleInput p/ 3º detector, rename do TTL, regex frágil do teste de integração. Lockstep 7 arquivos.) Anterior: **7.13.0** (2026-06-12, MINOR — Claude Code loop hardening: fecha os 13 achados (R1–R13) da auditoria `docs/specs/2026-06-12-cloudcode-loop-hardening/` em 7 batches B0–B6, cada um com ATDD+TDD + revisão adversarial zero-context (fix loop max 3). **B0** (R9/R2/R5): `scripts/run-tests.cjs` passa a varrer `tests/unit`/`tests/packaging`/`tests/watchdog` (antes órfãs) com timeout por teste + gate de categoria (vazia=skip, nunca falha); drift de versão corrigido (`lib/index.cjs` 6.1.0→canônico + README badge) + F7 estendido; frontmatter de `pipeline-controller.md` perde `Bash`+`Task` (o corpo já os negava) + scanner de consistência frontmatter↔prosa (F03). **B1** (R3, grave): `hooks/hooks.json` — o manifesto canônico que registra os 11 hooks de segurança — adicionado ao `package.json` `files[]`; o pacote empacotava os scripts mas NÃO o manifesto, então um install via tarball/npm subia SEM os hooks ativos; smoke test real via `npm pack`. **B2** (R4, grave, warn-first): `sentinel-state-signer.cjs` ganha canonicalização RECURSIVA (pega tampering aninhado que o `JSON.stringify(rest, topKeys.sort())` raso deixava passar), `timingSafeEqual` guardado por comprimento (não crasha o hook), chave HMAC versão-agnóstica com fallback à legada 5.3.0; `sentinel-hook.cjs` verifica assinatura — WARN por padrão + evento AUDIT, STRICT via `PIPELINE_HMAC_STRICT` (fail-closed cobre estado ausente/ilegível/adulterado). **B3** (R6, grave): `references/entry-points.json` + `lib/entry-points.cjs` (SSOT) consumidos por `session-lock-hook` e `force-pipeline-agents`, eliminando a regex duplicada que perdia `user-story`/`ux-sim`/`spec` e todos os `-light`/`-heavy`; fallback completo embutido se o registry corromper (nunca fail-open). **B4** (R8): novo `lib/exclusive-lock.cjs` (lock O_EXCL + stale-recovery por idade + guarda de relógio-futuro + `Atomics.wait`) compartilhado por gate-decisions/protocol-events/run-log; `safeAppendJsonl` passa a travar de verdade (o comentário "exclusive lock" virou verdade); protocol-events deixa o append cru. **B5** (R7/R10): `decision: FORCE_VARIANT_OVERRIDDEN` e o `PASS` de `STATE_FILE_INIT_FAIL` → `TRIGGERED` canônico (semântica vai pra `detail`); scanner de vocabulário com checagem de contexto gate-entry; `run-log` preserva `null` (desconhecido ≠ zero); doc cobertura-vs-qualidade. **B6** (R11/R12/R13/R1): `final-validator` deixa de tolerar SKIP adversarial silencioso em MEDIA/COMPLEXA (evento AUDIT `ADVERSARIAL_EVIDENCE_MISSING`, NÃO gate novo — Iron Law: registry de 35 + Inline Invariants intocados, padrão v7.9.4 PLAN_MODE_BYPASS); `scripts/watchdog-cloudcode-hardening.cjs` (W1–W12, fail-closed, allowlist explícita de baseline). 15 arquivos de teste novos (`tests/regression/v7.13.0/F01–F09` + `tests/packaging/` + `tests/watchdog/`); 6 ciclos de revisão adversarial zero-context (achados HIGH/CRITICAL incl. bypass do sentinel por estado corrompido em strict, fail-open de registry, false-PASS do watchdog por substring — todos corrigidos e re-confirmados). Espelhos `.codex` byte-idênticos. Iron Law preservada. Lockstep 7 arquivos.) Anterior: **7.12.0** (2026-06-11, MINOR — skill-dispatch wiring: completa o trabalho de variantes da v7.11.0 ligando os quatro fluxos que ainda rodavam a Fase 2 inline (`audit-*`, `feature-*`, `user-story-*`, `ux-sim-*`) ao despacho por skill no controller, junto com `bugfix-*` e `spec-*` que já eram despachados — só `DIRETO` continua inline. Novos atalhos por tipo `skills/user-story/` + `skills/ux-sim/` (espelham audit/bugfix/feature) com flags `--light`/`--heavy`. Contrato "state update before spawn" generalizado para QUALQUER variante com skill (sem enumeração que drifta). Design de dois níveis documentado: `--light/--heavy` no `/pipeline` central cobre só Bug Fix; outros tipos forçam profundidade pelos atalhos próprios. Reversão consciente do AUDIT-012: v7.11.0 reconciliou audit-heavy/light.md dizendo "inline" (casando com o controller da época); v7.12.0 resolve no outro sentido — controller AGORA despacha audit por skill e as docs dizem isso; tabela de composição também reconciliada (sem executor-controller na Fase 2 do audit report-only). Testes novos F31 (wiring, 9 cenários) + F32 (integridade da cadeia de passos user-story/ux-sim, 22 cenários); 2 passes de revisão adversarial (5 achados ARCH-1..5 corrigidos). Suíte sem falhas novas; Iron Law preservada (registry gates.md + 35-gate Mandatory table intocados; nenhum agente além do controller alterado). Lockstep 7 arquivos.) Anterior: **7.11.0** (2026-06-11, MINOR — enforcement-audit remediation: fecha os 17 achados da auditoria de enforcement+variantes de 2026-06-11 (`docs/audits/2026-06-11-enforcement-variants-audit/`) em 5 lotes, cada um com TDD onde houve código, 2 passes de revisão adversarial e watchdog de completude (18/18 dispostos). **B1 governança:** listas inline de gates sincronizadas 22/27→35 (AUDIT-008), audit-heavy/light.md corrigido para roteamento inline (AUDIT-012), Feature canonizado `feature-light`/`feature-heavy` com aliases `implement-*` deprecados (AUDIT-013), F20 cobre os 10 agentes (AUDIT-015a), Visible Progress Protocol obrigatório via TaskCreate/TaskUpdate (AUDIT-018), novo F24. **B2 telemetria:** run_id não-nulo nos spans Langfuse (fallback basename) + pipeline_doc_path relativo (AUDIT-003), `ensureFidelityReport` keep-max sem congelamento (AUDIT-004), timestamp_start derivado de pipeline_id + guarda clock-skew (AUDIT-010), F25; AUDIT-011 (chat-derek) documentado como poluição histórica zero-desde-F14. **B3 discovery:** F27 testes comportamentais do sentinel-hook (AUDIT-009) + ponteiro determinístico `<cwd>/.pipeline/active-run.json` com contenção/session-scope (AUDIT-005), F28. **B4 Plan Mode:** roster machine-readable `references/plan-mode-mandatory-agents.json` (AUDIT-002) + detecção determinística de bypass no dispatch-guard.cjs (PLAN_MODE_DISPATCH_PENDING/PLAN_MODE_BYPASS por código, warn-first + flag deny) (AUDIT-001/006), F29; AUDIT-016 ACCEPT anti-deadlock mantido como design. **B5 variantes:** skills user-story-light/heavy (porte 1:1 Pulsar, 10 passos) + ux-sim-light/heavy (sintetizadas report-only) fecham a camada 3 — 5 tipos agora 4/4 (AUDIT-014); enforcement sentinel ciente de variante via getVariantSkill (AUDIT-007), F30. 6 arquivos de regressão novos F24-F30 (75 cenários); suíte sem falhas novas vs baseline. Iron Law preservada: registry gates.md + 35-gate Mandatory table intocados; espelhos codex byte-idênticos. Lockstep 7 arquivos.) Anterior: **7.10.1** (2026-06-09, PATCH — parallel_eligible default contract: formaliza o default de parallel_eligible como false quando a análise de sobreposição não pode ser concluída. plan-architect ganha Regra 5 exigindo parallel_eligible: false com overlap_reason prefixado 'analysis_incomplete:' quando CHANGE_CONTRACT está ausente ou contém wildcards. executor-controller ganha guarda explícito tratando parallel_eligible ausente como false com WARN trace, convertendo comportamento implícito JS falsy em contrato observável. 2 cenários de regressão adicionados (F22-S10 + F22-S11, total 11). Dogfood validou Plan Mode protocol: 3/3 agentes obrigatórios emitiram PLAN_MODE_REQUEST na primeira tentativa. Iron Law preservada.) Anterior: **7.10.0** (2026-06-09, MINOR — Plan Mode generalized + parallel dispatch: generaliza Plan Mode obrigatório de 1 para 10 agentes (7 pesquisa + 2 implementadores + plan-architect), enforcement PLAN_MODE_BYPASS generalizado, despacho paralelo de tarefas em lotes MEDIA com verificação de sobreposição de escopo de arquivo, per_task_status condicional no checkpoint-validator, revisão adversarial com 6 fixes, 4 novos test files F20-F23 (74 cenários); Iron Law preservada.) Anterior: **7.9.4** (2026-06-06, PATCH — plan-mode contract fix via dogfood run (Bug Fix/MEDIA/bugfix-light, 3 batches, TDD 14 cenários RED→GREEN, final adversarial 3 revisores zero-context, 1 rework autorizado, Pa de Cal GO @ 0.94): fecha 21+ runs consecutivos sem Plan Mode. (a) `agents/quality/plan-architect.md` — corpo alinhado ao topo Achado #7: Step 0 emite `PLAN_MODE_REQUEST` e para; Step 3 emite `GATE_REQUEST`; Step 4 delega saída de plan-mode ao parent; rodapé Tools só Read/Grep/Glob + protocolo; 4 claims de header alinhados ao modelo delegado (prefixo do label Rule 1 "Read-only in Plan Mode" preservado verbatim — regressão D7-S7 v6.1.0 pega mid-run por verificação direta do controller e reconciliada com parentético). (b) `agents/core/pipeline-controller.md` — enforcement `PLAN_MODE_BYPASS`: plano inline sem ciclo REQUEST/RESULTS gera evento AUDIT em protocol-events.jsonl + um re-dispatch; aceita no segundo bypass (sem deadlock legado); NÃO é gate novo, registry 35 e Inline Invariants intocados. (c) `tests/regression/v7.9.4/` F18 (8 cenários, extractor fence-aware provado por mutação) + F19 (6 cenários) pinam o contrato. Suíte 62/67 = baseline pré-run idêntico (5 falhas Langfuse/env). Prova comportamental (UI do plan mode aparecer) é verificação do próximo run; evidência viva do 22º bypass preservada no protocol-events.jsonl do run. Iron Law preservada.) Anterior: **7.9.3** (2026-06-05, PATCH — telemetry recording fixes via dogfood run (Bug Fix/MEDIA/bugfix-light, 3 batches, Pa de Cal GO): (B1) guarda anti-duplicação `shouldAppendRunLogEntry` no stop-hook — compara só os 8 campos materiais contra a última entrada do mesmo run_id, mata o re-append por teardown (run de 31/mai tinha 30 linhas quase idênticas), F15 7/7; (B2) `ensureFidelityReport` idempotente no stop-hook ANTES de buildRunLogEntry + helper `readClassification` (DRY do D2) — run-log passa a carregar fidelity_score numérico, F16 6/6; (B3) enriquecimento de traces Langfuse — `readSentinelData` (precedência PIPELINE_DOC_PATH > PIPELINE_RUN_ID > mtime), `buildTraceMetadata` (run_id/type/complexity/phase no trace E span, sanitizado), `resolveSpanName` (fim dos spans "unknown" + AUDIT SPAN_NAME_FALLBACK), e tier doc-path estável no carrier `_resolveKey` entre sessionId e ppid (AUDIT CARRIER_DOCPATH_FALLBACK) — mata a fragmentação 1-run→N-traces, F17 8/8; inclui também o F14 in-flight (Langfuse project isolation no langfuse-client via .env do plugin). Espelhos codex byte-idênticos; Iron Law preservada; suíte alvo verde, baseline in-flight provado alheio via arbitragem git-stash A/B.) Anterior: **7.9.2** (2026-06-02, PATCH — documentation refresh, docs-only: atualiza README + diagramas (v7.4.0/v6.0.0 → v7.9.2, incl. correção da licença MIT → PolyForm Shield nos diagramas), documenta a camada Paperclip completa (integration layer v7.6.0 + company provisioner v7.8.0 + flow-mirror v7.9.0 com árvore de issues + 8 comandos paperclip-* + flag --on=paperclip + novo `references/paperclip/PAPERCLIP-FLOW-MIRROR.md`), atualiza a seção run-log do audit-trail para o modelo v7.9.1 (buildRunLogEntry derivando total_gates_expected/fidelity_score/final_decision), alinha o vocabulário de decisão de gate aos 8 valores canônicos e corrige drift de contagens (gate registry header 22→35, Mandatory table 22→23, suíte 61). ZERO mudanças em código de runtime; Iron Law preservada.) Anterior: **7.9.1** (2026-06-02, PATCH — telemetry run-log aggregator bugfix: buildRunLogEntry (stop-hook + espelho .codex) passa a ler classificação de sentinel.orchestrator_decision em vez do campo errado (D2), tratar duração date-only como null em vez de inflar para horas (D4), derivar total_gates_expected de MANDATORY_GATES_BY_COMPLEXITY (MEDIA=11) + fidelity_score do fidelity-report.json do run (D1), e derivar final_decision real (CLOSEOUT_CONFIRM/Pa de Cal) antes de cair em UNKNOWN (D3); novo teste F13 15/15 RED→GREEN, suíte 61/61, espelho byte-idêntico (sha256); Option A: escopo travado em 2 hooks + 1 teste, lib/run-log.cjs intocado; Iron Law preservada, Mandatory Gates + Registry intactos.) Anterior: **7.9.0** (2026-06-01, MINOR — Paperclip flow-mirror: árvore de tarefas + 8 comandos slash por fluxo que espelham os 14 fluxos do pipeline no Paperclip via árvore de issues com travas `blockedByIssueIds`; novos módulos em `references/paperclip/spec/lib/` (tree-template/tree-factory/tree-factory-io/grow-tree/classify-bridge) + patch `CLARIFICATION_DONE` na régua + 8 comandos `paperclip-*` com guard dry-run/--confirm; suíte mirror-fidelity 63→309; aditivo, Iron Law preservada.) Anterior: **7.8.0** (2026-05-26, MINOR - Paperclip company provisioner: `references/paperclip/scripts/provision-pipeline-company.cjs` cria uma empresa Paperclip com os 47 cargos + 11 skills, ID-agnostic (empresa por nome, paths via __dirname, sem UUIDs fixos), batimento desligado, amarrado em `/pipeline-orchestrator:setup-paperclip`; additive script+doc, Iron Law preservada.) Anterior: **v7.7.0** (2026-05-25, MINOR - OpenCode adaptation marketplace release: opencode-adaptation/ publicado com harness, comandos, agentes, skills e testes de contrato para OpenCode; workflows exigem evidencia fisica e comandos globais audit/UX/SPEC foram adicionados.) Anterior: **v7.6.1** (2026-05-24, PATCH — release hygiene closure: CLAUDE.md canonical line + cursor manifest description + F7 version-sync test atualizados, fechando drift detectado durante v7.6.0 npm publish. ZERO runtime changes, suite 58/59 GREEN, Iron Law preservada). Anterior: **v7.6.0** (2026-05-22, MINOR — Paperclip integration layer + User Score Collection. Paperclip layer: `references/paperclip/` (14 docs + 11 skills custom) + `/pipeline-orchestrator:setup-paperclip` slash command + `docs/PAPERCLIP-INTEGRATION.md` overview permitem rodar o plugin dentro do Paperclip (open-source agent orchestration — paperclip.ing) via adapter `claude_local` com paridade efetiva ao Claude Code standalone. PARITY_VERDICT=PASS_WITH_NOTES validado em pilot E2E. Iron Law preservada: ZERO mudanças em `agents/`, `skills/`, `references/` originais, `commands/` originais ou `lib/`; tudo additivo. NPM destravado em 2026-05-24 (granular token com Bypass 2FA). Anterior: **v7.5.0** (2026-05-22, MINOR — concurrent-safe tracing: RunDirectory.allocate com exclusive mkdir + collision-resistant uniqueId publicado em PIPELINE_RUN_ID; Langfuse carriers + sentinel/stop discovery rechaveados por runId com fallback graceful PPID/mtime + AUDIT breadcrumbs; PIPELINE_TRACING_SCOPE env-var governa escopo de observação no Langfuse; 5 novos regression tests F1-F5; Codex mirror byte-identical). Lineage: v4.2.x → v4.3.x → v4.4.x → v4.5.0 → v4.6.x → v4.7.0 → v4.8.0 → v4.9.x → v4.10.0 → v4.11.0 → v4.12.0 → v4.13.0 → v4.14.0 → v4.15.0 → v4.16.0 → v4.17.0 → **v5.0.0** (2026-05-06, first major) → v5.1.0 (brainstorm + Kiro skill clone) → v5.2.0-rc.1 (Achado #7 GATE_REQUEST/DISPATCH_REQUEST protocol) → v5.2.0-rc.2 (Achado #7 expanded to 5 SKILLs + brainstorm steps + 9 dispatch sites) → **v5.2.0** (2026-05-08, promoted from rc.2) → **v6.0.0** (2026-05-15, MAJOR — comprehensive audit + hardening: 12/12 CRITICAL findings resolved, 22/22 gates runtime tested, 3 new security libraries (HMAC signer + JSONL sanitizer + pipeline.local sandbox parser), 39 regression tests in tests/regression/v6.0.0/, audit reports in docs/audits/2026-05-15-ifrs16-deep-audit/, visual documentation in docs/diagrams/, STALE constants renamed to canonical names with deprecated aliases. Coverage 30% → 95%.) → v6.1.0 (2026-05-15, MINOR — ATDD/BDD/DDD workflow contracts: D5 non-spec Given/When/Then in quality-gate-router Step 1b, D6 BDD Gherkin per slice in feature-vertical-slice-planner Step 3b with flat naming, D7 DDD lightweight Bounded Contexts in plan-architect with SOFT BOUNDED_CONTEXT_MISSING event using canonical protocol-events.jsonl schema. 4 new regression tests in tests/regression/v6.1.0/.) → **v6.2.0 (2026-05-18, MINOR — clarification overhaul: step-01-explore reescrito como agente dinâmico de 4 fases substituindo template fixo de 7 perguntas (Phase A read-first project context; Phase B 11-lens gap inventory; Phase C one GATE_REQUEST per real gap with mandatory file:line evidence; Phase D record + telemetry). Novo step-01b-alternatives propõe 2-4 abordagens (Minimal/Pattern-aligned/Aggressive/Contrarian) com auto-skip para tarefas mecânicas. Drift fix em complexity-matrix.md (Has spec MEDIA: Optional → Required (auto-dispatch)). +5 gates + nova classe AUDIT hardness; registro 22 → 27. Brainstorm controller table 9 → 10 steps. Dogfood evidence em docs/audits/2026-05-18-clarification-overhaul-dogfood/ (real IFRS-16 scenario, 14 telemetry events, 4 anti-invention blockers surfaced).)** → **v6.3.0 (2026-05-19, MINOR — Implementation Discipline Layer: new SSOT `references/implementation-discipline.md` (scope control, minimal diff, SOLID/KISS/DRY/YAGNI, dep/config/contract/migration restrictions, test integrity, evidence, severity PASS/NEEDS_REDUCTION/REJECTED); CHANGE_CONTRACT inline em plan-architect.md (allowed/forbidden files, forbidden_change_types, diff_budget, bootstrap); SCOPE LOCK CHECK em executor-implementer-task.md (fires before every Write/Edit; Read free); novo agente `agents/quality/diff-discipline-reviewer.md` (tools=Read/Grep/Glob, static-only, max_fix_attempts=5 independente do ADVERSARIAL_BLOCK max=3); 3-way parallel em review-orchestrator (adversarial-batch + architecture-reviewer + diff-discipline-reviewer) com fix_loop_counters duais; architecture-reviewer +5 novos checks (abstraction-without-second-use, new-file-where-local-suffices, unnecessary-layer, unrelated-refactor, public-contract-drift); 7 regression tests novos (F8-F14) em tests/regression/v6.3.0/. **Invariantes preservadas:** 22-row Mandatory Gates by Complexity untouched (F1 intacto); 32-row Gate Registry untouched (F14 defense-in-depth). **Roster 19 → 20 agentes** com diff-discipline-reviewer. **Bootstrap exception** one-time: tasks T1-T3 de v6.3.0 rodaram sem enforcement runtime (mecanismo nascendo); T4+ enforced. Setting bootstrap.active=true em planos futuros é forbidden_change_type. Backward compat: plans pré-v6.3.0 sem CHANGE_CONTRACT skipam diff-discipline cleanly; v6.4.0 promoverá HARD.)** → **v7.0.0 (2026-05-19, MAJOR — LICENSE-ONLY: PolyForm Shield 1.0.0 (source-available com noncompete clause). Bloqueia revenda comercial + managed-service competidor; permite uso interno/pessoal/educacional/modificação/redistribuição. Nenhuma mudança de código vs v6.3.0; o bump MAJOR sinaliza license change per SemVer convention. Trademark: "Pipeline Orchestrator" + "FX Studio AI" reservados — forks devem renomear. NOTICE.md documenta atribuição obrigatória, proibição de falsificar autoria, e política de enforcement (DMCA, cease-and-desist, terminação de licença, ação civil). Marketplace + NPM canais inalterados, ambos sob Shield.)** → v7.1.0 (2026-05-19, MINOR — telemetry hygiene) → v7.1.1 (2026-05-21, PATCH — docs only) → **v7.2.0 (2026-05-21, MINOR — Phase 0 hardening: 4 patches commitados em batches separados com ATDD/BDD/DDD per scenario + adversarial review per batch (22 findings: 3 CRITICAL fixed, 12 HIGH fixed, 5 MEDIUM fixed). Patch 2 — STATE_FILE_INIT_FAIL (CIRCUIT_BREAKER, mandatory across complexity): hard precondition no initial sentinel-state.json write; controller emits PASS no caminho saudável AND BLOCKED em falha de Write/verify ANTES de qualquer Agent spawn — sem PASS verdicts sobre estado ausente. Patch 3 — PROTOCOL_HANDSHAKE_TIMEOUT (HARD): timeout em duas camadas (out-of-band SessionStart hook + secondary controller re-entry check); default 30min; env override PIPELINE_HANDSHAKE_TIMEOUT_MS com 60000ms sanity floor; cross-session demotion para SOFT/CLEARED_CROSS_SESSION; clock-skew + MAX_AGE defenses; closes Achado #7 silent-deadlock circularity. Patch 5 — cleanup-orphan-sentinel-state-hook (.claude/hooks/, SessionStart): auto-archive de sentinel-state.json >24h via atomic rename para sentinel-state.archived-<ts>.json (closes Achado B1-001 manual workaround); sandbox cwd (deny-list + realpath), depth-limit walk=10, symlink-skip, mtime CAS race-defense. Patch 4 — --strict-spec flag: rejeita Signal 3 (prose regex) e Signal 4 (glob fallback) em unattended/CI mode; preserva Signal 1 (path arg) e Signal 2 (--type=spec); prefix-line anti-injection; precedência sobre conflicting FORCE_VARIANT=spec-*; STRICT_SPEC_REJECTION gate (AUDIT). Gate Registry 32 → 35 rows (Core 16 → 18 + Routing 3 → 4). Mandatory Gates by Complexity 22 → 23 rows. Inline Invariants atualizadas com STATE_FILE_INIT_FAIL e PROTOCOL_HANDSHAKE_TIMEOUT. Suite 45/46 verde (F7 version-sync atualizado para v7.2.0). All changes additive com backward compat preservado.)**. Marketplace lê este arquivo. |
| **Pipeline flow** | `commands/pipeline.md` (full classifier) + `commands/bugfix.md` (thin shortcut) | 1112 linhas no full + 40 no thin. **Inline Invariants** em `pipeline.md` sobrepõem Grep-loaded refs em caso de conflito |
| **Agent orchestrator** | `agents/core/pipeline-controller.md` | N1, dispatched pela skill, coordena 4 fases |
| **Gates** | `references/gates.md` | Hardness Taxonomy + 35-gate Registry |
| **Audit Trail** | `references/audit-trail.md` | Phase Transition Summary + JSONL log format |
| **Confidence** | `references/confidence.md` | Scoring schema (advisory) |
| **Sentinel** | `references/sentinel-integration.md` + `agents/core/sentinel.md` | 5 mandatory checkpoints |
| **Team roster** | `references/team-registry.md` | **20 agents reais** [VERIFICADO 2026-05-19, v6.3.0 added `diff-discipline-reviewer`] (`agents/**/*.md`). O número antigo "38" estava errado — vinha do D1 sem reality-check; corrigido em `designs/pipeline-orchestrator-v5-consolidated.md` §1.1 e §16 erro #4. `team-registry.md` pode ainda mostrar 38 — atualizar quando tocado. |

## Política de edits

🔴 **REGRA #1:** Todas as alterações nascem em `D:\Pipeline Orchestrator Claude\Pipeline-Orchestrator` (este repo).

🔴 **REGRA #2:** O cache `C:\.claude\plugins\cache\FX-studio-AI\pipeline-orchestrator\<version>\` é **runtime read-only** — NUNCA editar lá. Ele é populado pelo marketplace ao instalar/atualizar.

🟡 **HISTÓRICO:** Antes de 2026-04-30, releases 3.8.0 → 4.1.3 foram publicadas no marketplace SEM passar pelo git canonical (deriva de 97 commits). Em 2026-04-30, canonical foi sincronizado com cache 4.1.3 via 5 commits temáticos (`59e0984..c5c9477` em `feat/v5-addendum-context-policies`). Mesmo dia: Slice 0.5 + Slice 1 (`/bugfix` entry-point) entregues como v4.2.0 + patch v4.2.1 (SEC-1+SEC-2), seguidos de v4.3.0 (skill migration), v4.3.1 (review polish), v4.4.0 (Slice 1.5 Pulsar bugfix import) em 2026-05-01, v4.4.1 (exec-window wrapper) e v4.5.0 (Slice 3a Pulsar audit import) em 2026-05-02. Daqui em diante, **todo release passa pelo git** antes de virar marketplace package.

## Política operacional para Claude (este agent)

1. **Edits via Agent tool durante execução de pipeline.** Quando dentro do pipeline (skill `pipeline` ativada), o `pipeline-controller` é o N1 e dispatcha N2/N3. NUNCA editar inline durante execução.
2. **Edits diretos OK fora do pipeline.** Ad-hoc fixes, doc updates, design iteração — usar Edit/Write direto.
3. **Espelhamento Codex:** `.codex/hooks/force-pipeline-agents.cjs` deve seguir mudanças em `.claude/hooks/force-pipeline-agents.cjs` (manter espelho funcional).
4. **Untracked locais:** `.kiro/`, `.pipeline/` são working-state — NUNCA commitar.
5. **Designs preservados:** `designs/`, `designs/legacy/`, `designs/slice-0/` são history do design v5 — preservar mesmo durante syncs.

## Invariantes resumidos

Os 13 invariantes operacionais vivem em `commands/pipeline.md` (CRITICAL REMINDERS section). Em resumo:
1. Single PIPELINE_DOC_PATH + sentinel state file
2. TDD mandatory para code-changing pipelines
3. Non-Invention + Proportionality
4. User approval em gates específicos
5. AskUserQuestion sempre (nunca prosa para decisão)
6. Automatic batching por complexity
7. Per-batch adversarial + Fix loop max 3
8. Stop rule + phase rollback
9. Review independence (zero-context)
10. Parallel reviewers em final adversarial
11. Verification-before-claim
12. Gate decision log + confidence score (advisory)
13. Sentinel state tracking via PreToolUse hook

Ver `commands/pipeline.md` linhas finais para detalhamento.

## Glossário e referência cruzada

- `references/glossary.md` — termos canônicos (Pipeline, Phase, Gate, Adversarial, etc.)
- `references/complexity-matrix.md` — Pipeline Routing Matrix + Proportional Behavior
- `references/audit-depth-levels.md` — níveis 1/2/3 de auditoria (R-3 do consolidated)
- `references/feature-light-criteria-checklist.md` — ownership de acceptance criteria em feature-light v5.0 (R-5)
- `docs/MIGRATION-v3-to-v4.md` — guia de migração v3 → v4
- `docs/assets/sentinel-philosophy.md` — fundamentos de design do sentinel

## Ponteiros futuros

- `docs/migration-v4-to-v5.md` — a ser criado quando v5.0 for lançado (Slice 4 / DoD)
- `references/trace-schema/v1.md` — a ser criado no Slice 2 (TRACE.md generator)

## Skill governance

Regras de versionamento de skills, lockstep entre manifests Claude/Cursor, padrão de divulgação progressiva, e rastreamento de adoção de padrões externos vivem em [`references/skill-governance.md`](references/skill-governance.md). Consultar antes de bumpar versão de skill ou refatorar conteúdo.

## Release / Deploy (full deploy canônico)

Quando o pedido for "deploy completo / o que você sempre faz", são **4 canais** — pular qualquer um deixa o release pela metade. O erro recorrente: bumpar código+GitHub mas **não** atualizar o cache global, deixando o Claude Code rodando a versão antiga em todo terminal (foi a causa raiz de deadlocks com o cache preso enquanto a fonte já estava à frente).

**Passo 0 — bump lockstep (neste repo) antes de qualquer canal.** Casar TODAS as superfícies de versão; `tests/regression/v7.1.0/F7_version_sync.cjs` valida 8 delas, e os pins-espelho `tests/regression/v8.5.0/F6-registration-lockstep.test.cjs` (`VERSION`) e `tests/regression/v8.8.0/F01_refactor_slice1.cjs` (`PINNED_VERSION`) também travam a versão — bumpe os três. Superfícies: `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` (version **+** `source.ref` `vX`), `.cursor-plugin/plugin.json`, `package.json` (version + description), `lib/index.cjs`, `hooks/hooks.json` (banner "vX loaded"), `README.md` (badge), `CLAUDE.md` (linha `Atualmente: **X**` + nova linha na tabela), nova entrada no `CHANGELOG.md`. **NÃO** tocar `.claude/hooks/enforcement-surface-verify-hook.cjs` — os `8.18.1` lá são comentários de proveniência (ARCH-001/002), mudá-los falsearia o histórico. Rodar `node scripts/run-tests.cjs` e confirmar baseline (atual: 5 falhas pré-existentes — 3 Langfuse + score-writer + F4 dispatch-pending-gate herdado da v8.18.0).

**Os 4 canais (nesta ordem):**
1. **GitHub canônico** — commit + `git tag vX.Y.Z` + push de `main` e da tag para `fernandoxavier02/Pipeline-Orchestrator`.
2. **Cache global (o passo sempre esquecido)** — o marketplace `FX-Studio-AI` **não** tem `autoUpdate`, então o cache em `~/.claude/plugins/cache/FX-Studio-AI/pipeline-orchestrator/<versão>/` fica congelado até ação manual. Instalar a nova versão nesse cache e apontar `~/.claude/plugins/installed_plugins.json` para ela (installPath/version/lastUpdated/gitCommitSha). **É isto que faz o CC carregar a versão nova em qualquer projeto.**
3. **NPM** — `npm publish` do pacote `@fx-studio-ai/pipeline-orchestrator`.
4. **Marketplace** — no clone do repo separado `~/.claude/plugins/marketplaces/FX-Studio-AI` (remote `fernandoxavier02/FX-Studio-AI`), editar a entrada pipeline-orchestrator em `.claude-plugin/marketplace.json` (version + `source.ref` → nova tag) + commit + push. Canal público/outward-facing — confirmação de segurança esperada.

Push ao GitHub, `npm publish` e push ao marketplace são ações públicas/irreversíveis — confirmar no momento se houver prompt.

## Versão deste arquivo

| Versão | Data | Mudança |
|---|---|---|
| v3.9 | 2026-06-30 | Bump 8.19.0 → 8.19.1 (PATCH — arm-marker consume-on-close deadlock fix). O marcador `pipeline-arm-pending` não era consumido quando um run governado encerrava de propósito (só TTL 30 min ou restart o limpava), então um run FULL/código que armou + concluiu deixava o marcador vivo e o arm-gate bloqueava a próxima tarefa não-pipeline da mesma sessão com `PIPELINE_NOT_ARMED` (REVIEW-ONLY/DIAGNOSTIC nunca armam). FIX: `stop-gate-hook.cjs` consome o marcador nos caminhos de ALLOW de fechamento (completo/GO/spec-selado/hard_failed-cap/inativo) via novo `lib/pipeline-arm.cjs::clearArmPendingForSession` — nunca em block/recovery/corrupt. Delete por posse-positiva (session_id ambos presentes e iguais, OU ambos ausentes; ambíguo→preservado pro TTL) + veredicto de tamper delegado ao SSOT `lib/arm-pending`. 2 rondas adversariais zero-context (1 HIGH + 1 CRÍTICO corrigidos) → CLEAN. TDD `tests/regression/v8.19.1/F1` (10 cenários, 5 call sites + tamper + posse). Iron Law: só `.claude/hooks/stop-gate-hook.cjs` + `lib/pipeline-arm.cjs` + `tests/`; Mandatory Gates table + Gate Registry INTOCADAS; sem espelho `.codex`. Suíte 250/5. Lockstep 9 superfícies. |
| v3.8 | 2026-06-29 | Bump 8.18.2 → 8.19.0 (MINOR — spec-authoring output enforcement). Fecha o gap de canal-de-retorno do spec-authoring: o seal já era code-enforced mas o contrato de o spec-controller emitir um bloco terminal + o parent expô-lo era honor-only. (A) `spec-controller` Step 9 termina num bloco cercado `PIPELINE_AGENT_RESULT_V1`; (B) `spec-controller` vira folha Spec governada em `lib/contracts/workflow-manifest.cjs` resolvida por `spec_authoring_progress`, policiada pelo SubagentStop check já armado (comentário stale DEFAULT-OFF corrigido); (C) o Stop gate fecha um run Spec só com evidência de seal em disco, não por inatividade (HIGH-1). Emit-and-hoist safety: stops intermediários `AWAITING_*` passam un-counted, só o terminal é policiado. Seal-forgery guard: autoridade do seal consolidada no `final_decision` do sentinel assinado (status do `manifest.yaml` não-assinado deixa de ser concessão independente) via `workflow-manifest.cjs::sealEvidence`. Loop adversarial convergiu (1 CRÍTICO hoist-cap + 1 LOW over-match + 1 LOW manifest-forgery, todos corrigidos). Testes `tests/regression/v8.19.0/F1-F8` + contrato do manifesto. Iron Law: 2 hooks + `spec-controller.md` + contrato do manifesto + tests; Mandatory Gates table + Gate Registry INTOCADAS; sem espelho `.codex`. Lockstep 9 superfícies. |
| v3.7 | 2026-06-29 | Bump 8.18.1 → 8.18.2 (PATCH — observer governance guard). `corrective-error-signal-gate.cjs` emitia `SUBAGENTSTOP_RESULT_MISSING` para qualquer `agent_type` (incl. agentes do harness `Explore`/`general-purpose` fora do pipeline → falso positivo + corretiva espúria, observado ao vivo com run_id:""). Fix: `stepForAgentType` promovido ao SSOT `lib/contracts/workflow-manifest.cjs` + novo `isGovernedLeaf`, consumido pelo observador (guard membership-by-name) e pelo `subagent-stop-commit-hook.cjs` (delega ao manifesto). +tests/regression/v8.18.2/F1 (RED→GREEN) + S1-14..S1-17 no contrato do manifesto (9 folhas + null-spine + falsy + trava de paridade manifesto↔step-ledger). Revisão adversarial 3 lentes zero-context em loop até CLEAN; nenhum CRÍTICO/ALTO. Iron Law: só `.claude/hooks/` + `lib/` + `tests/`; Mandatory Gates table + Gate Registry INTOCADAS. Suíte 241/5 (5 pré-existentes). Lockstep 9 superfícies. |
| v3.6 | 2026-06-28 | Bump 8.15.1 → 8.16.0 (MINOR — autonomous bugfix of 2 glitches caught by the v8.15.0 E2E evaluator during the v8.15.1 self-audit run). Glitch A (`missing_step:final`) is PINNED by F1 (4 cases) protecting the existing `step-ledger-stamp.cjs` contract; Glitch B (`BRAINSTORM_EVIDENCE_MISSING`) closed by adding an idempotent `STEP_1_7_ROUTING(no-prep-override)` auto-emit to the same hook on the first governed step of a direct-entry MEDIA/COMPLEXA/Spec run (skipped when a `step_1_7` block or gate entry already exists). Both via the SSOT `lib/step-1-7-routing.cjs`. Suite 234/4 (baseline 232 + 2 new files; the 4 pre-existing Langfuse/env failures unchanged). Iron Law preserved: only `.claude/hooks/step-ledger-stamp.cjs` + `tests/` touched; agents/skills/references/lib untouched; the 35-row Mandatory Gates table + 49-row Gate Registry UNTOUCHED. |
| v3.5 | 2026-06-27 | Bump 8.15.0 → 8.15.1 (PATCH — audit read-budget dispatch-exemption persistence fix). `lib/audit-read-budget.cjs::writeLedger` passa a preservar o flag `dispatched` entre escritas (o `bumpLedger` do contador PostToolUse apagava a isenção de `markDispatched` na 1ª leitura, estrangulando subagentes de review despachados). TDD via `tests/regression/v8.14.0/F11` (+bloco de regressão); revisão adversarial 0 crítico/0 alto/0 médio; suíte 232/4. Iron Law: só `lib/` + `tests/`. Release em todos os canais (GitHub/marketplace/npm/cache global). |
| v1.0 | 2026-04-30 | Inicial — pós-sync 4.1.3 (Slice 0.5 A1) |
| v1.1 | 2026-05-03 | Bump versão canônica 4.4.0 → 4.5.0; lineage estendida com v4.3.x/4.4.x/4.5.0; corrigida contagem do consolidated (1491 → 1796 linhas). Reconciliação de governança disparada por audit do design v5. |
| v1.2 | 2026-05-03 | Team roster corrigido (38 → 19 agents reais [VERIFICADO]) após coherence review apontar drift. |
| v1.3 | 2026-05-03 | Bump versão canônica 4.5.0 → 4.6.0 (Slice 3b — feature skill com mode flag, pattern novo vs Slice 1.5/3a). |
| v1.4 | 2026-05-03 | CORREÇÃO: bump 4.6.0 → 4.6.1. Decisão de "single skill com mode flag" foi revertida — port deve espelhar EXATAMENTE a fonte canônica (Pulsar Heavy/ + Ligth/ = 2 pastas → skills/feature-light/ + skills/feature-heavy/ separadas). Pattern volta a ser igual a Slice 1.5/3a. |
| v1.5 | 2026-05-03 | POLISH: bump 4.6.1 → 4.7.0. Slice 3b polido com 11 fixes audit-driven: contrato declarativo completo nos 26 step files (execution_mode + agent_type + expected_*), SKILL.md manifests completos (sequence_lock + gates_at + sentinel_checkpoints), thin entry-point `/pipeline-orchestrator:feature` criado (espelhando /audit + /bugfix), tests reais substituindo placeholder TESTS_USER_STORY (que virou `references/feature-user-story-guidelines.md`), deprecated headers corrigidos (apontavam para `feature.md` deletado), AGENTS.md count fix (38 → 19), correction headers em spec/plan docs. Skill funcional + 1:1 fidelity Pulsar preservada (texto dos prompts intocado). |
| v1.6 | 2026-05-03 | ENFORCEMENT: bump 4.7.0 → 4.8.0. Hooks NÃO enforce SKILL.md frontmatter (consolidated.md §17.4 #8) RESOLVIDO. Adicionado shared module `.claude/hooks/skill-frontmatter-parser.cjs` (parser YAML + getCurrentSkill + getEnforcementMode + logEnforcementDecision) consumido pelos 3 hooks: sentinel-hook valida `sentinel_checkpoints`, dispatch-guard valida `agent_type` per step (matcher agora `Skill\|Agent`), force-pipeline-agents valida `gates_at`. Roll-out 2-fases time-based: warn mode até 2026-05-17 (logga ENFORCEMENT_WARN em gate-decisions.jsonl com hardness AUDIT, não bloqueia), deny mode auto-promove (bloqueia via permissionDecision). Override via env `PIPELINE_ENFORCEMENT={warn,deny}`. Backward compat: hooks skipam silently quando state.current_skill ausente. Controller (`agents/core/pipeline-controller.md`) atualizado para popular current_skill + current_step antes de spawn. Iron Law respected: só hooks JS + tests modificados. |
| v1.7 | 2026-05-05 | Bump 4.10.0 → 4.11.0 (Wave 3-spec) — type=Spec first-class classification, 4-signal detection, routing dispatch in pipeline-controller, 5 sentinel checkpoints, AC-seeded ATDD in quality-gate-router. 4 core agents wired to existing Wave 1+2 spec lifecycle skills. Backward compat preserved. |
| v1.8 | 2026-05-05 | Bump 4.11.0 → 4.13.0 (skipping intermediate 4.12.0 lineage already in row 21). Wave 5-spec: gates registry from 16→22, Inline Invariants from 15→21 names, complexity-matrix routing for type=Spec, mode_used persistence in spec-closer, +16 unit tests. ARCH-1 drift between pipeline-controller.md Inline Invariants and references/gates.md Registry resolved. |
| v1.9 | 2026-05-06 | Bump 4.13.0 → 4.17.0 (intermediate v4.14/v4.15/v4.16 covered in lineage row). Wave 8-spec: TRACE.md schema + writer + standalone validator + plan-mode auto-trigger on MEDIA + `--no-plan` flag with asymmetric override semantics (COMPLEXA ignores it, plan runs anyway, override logged in TRACE). Closes DoD #2 #3 #6 in one wave. +7 integration tests, +5+5 unit tests. Suite 149/0/1 → 166/0/1. **DoD #5 closed post-tag** via [milestone #1](https://github.com/fernandoxavier02/Pipeline-Orchestrator/milestone/1) "v5.1 — Pipeline Declarativo (Slice 5)" (target Q3 2026) + 3 placeholder issues (#16 YAML grammar, #17 dispatch table, #18 interpreter spike). v5.0.0-rc.1 fully unblocked. Self-hosted dogfood revealed sentinel-hook cwd-discovery brittleness — flagged as v5.0-rc.1 follow-up. |
| v2.0 | 2026-05-06 | **Promote v5.0.0-rc.1 → v5.0.0** (first major release). Same content as rc.1; user waived feedback window. plugin.json + marketplace.json + test pin bumped 4.17.0 → 5.0.0. Marketplace.json `ref` bumped to `v5.0.0`. CHANGELOG `[5.0.0]` section added. All 7 v5.0 DoD criteria DONE. Suite remains GREEN at 166/0/1 + 5/1 mock + 8/8 hooks (zero regressions vs v4.16.0 baseline of 149). |
| v2.1 | 2026-05-15 | Bump 6.0.0 → 6.1.0 (v6.1.0 release). D5 (ATDD non-spec branch in quality-gate-router Step 1b, EARS And/But), D6 (BDD Gherkin per slice in feature-vertical-slice-planner Step 3b, flat naming), D7 (DDD lightweight Bounded Contexts in plan-architect, SOFT BOUNDED_CONTEXT_MISSING event with canonical schema). 4 new regression tests v6.1.0. Suite 17 → 19 GREEN. Forward dependencies tracked for v6.2: bdd_artifact consumer, gherkin_content persist, SLICE-NN >=100. |
| v2.2 | 2026-05-18 | Bump 6.1.0 → 6.2.0 (v6.2.0 release). Clarification overhaul: `step-01-explore` reescrito do template fixo de 7 perguntas para agente dinâmico de 4 fases (A read-first, B 11-lens gap inventory, C evidence-cited GATE_REQUEST per gap, D record + telemetry); novo `step-01b-alternatives` propõe 2-4 abordagens (Minimal/Pattern/Aggressive/Contrarian) com auto-skip para tarefas mecânicas; drift fix em `references/complexity-matrix.md` (Has spec MEDIA: Optional → Required (auto-dispatch)); +5 gates + nova classe AUDIT hardness; registro 22 → 27 gates; brainstorm controller table 9 → 10. Dogfood evidence em `docs/audits/2026-05-18-clarification-overhaul-dogfood/` (cenário real contra IFRS 16 contracts.py:232, 14 eventos de telemetria, 4 inventions prevented). Backward compat preservada. |
| v2.4 | 2026-05-21 | Bump 7.1.0 → 7.1.1 (v7.1.1 release, PATCH, docs only). Enrichment of `docs/diagrams/pipeline-overview.html`: new "Fluxos detalhados ponta a ponta" section covering 7 workflows E2E (bugfix light/heavy, audit light/heavy, UX light/heavy, plan, brainstorm, hotfix, review-only), 12 inline SVG step diagrams (color-coded agents + gates), Thariq-style interactive layer (sticky TOC pills, light/heavy filter pills, per-flow review checkboxes + Copy summary buttons with localStorage, collapsible exemplo cards, 11-option response form with Save/Copy/Clear, live `N/7` counter, TOC pills marked ✓ when reviewed). 380 → 1498 lines net. ZERO changes to runtime (agents/skills/references/commands/hooks/lib/tests untouched); 22-row Mandatory Gates and 27-row Registry preserved. Iron Law respected. |
| v2.3 | 2026-05-19 | Bump 7.0.0 → 7.1.0 (v7.1.0 release). Telemetry hygiene: new `lib/gate-decision-writer.cjs` SSOT writer (8-value canonical decision vocab BLOCKED/DISPATCHED/SKIPPED/APPROVED/CONFIRMED/REJECTED/TRIGGERED/NOT_TRIGGERED + auto-correlation run_id/plugin_version/schema_version/type/complexity; runtime throws on unknown values). Migrates 8 write sites in `lib/codex-operational-runtime.cjs` to use the new helper via local `logGateDecision` wrapper. `lib/fidelity-reporter.cjs` widens VALID_HARDNESS to include AUDIT (was missing since v6.2.0) and VALID_DECISION to all 8 canonical values; `validate*` now pushes a warning to fidelity-report's `warnings[]` when an invalid value is encountered (was silently nullified). New `.claude/hooks/stop-hook.cjs` (registered in `.claude/settings.json` Stop event) calls `lib/run-log.cjs::appendRunLog` on every session end including crashes — closes the 1-line-per-20-runs aggregate bug. `lib/jsonl-sanitizer.cjs` ALLOWED_GATE_DECISION_KEYS extended with the 5 correlation fields. `agents/quality/diff-discipline-reviewer.md` gains a Step 4b SSOT-bypass static check. 4 agent prose files (`finishing-branch`, `pipeline-controller`, `spec-closer`, `step-01b-alternatives`) reference the canonical vocab. 7 regression tests in `tests/regression/v7.1.0/` (F1-F7). Invariants preserved: 22-row Mandatory Gates by Complexity table untouched; 27-row Registry untouched. Backward compat: historical events still tolerated as `(invalid)`; no migration. Deferred to v7.2.0: Finding #3 (gate rename CHECKPOINT_FAIL/PLAN_REJECTED) and Finding #5 (timestamp format normalization). |
| v2.5 | 2026-05-21 | Bump 7.2.0 → 7.4.0 — cobre Langfuse v7.3.0 (commit 8a18dd1, observabilidade opt-in) + skills-patterns adoption (references/skill-governance.md + .cursor-plugin/plugin.json mirror, padrões importados de langfuse/skills). |
| v2.9 | 2026-05-25 | Bump 7.6.1 → 7.7.0 (MINOR — OpenCode adaptation marketplace release). Publica `opencode-adaptation/`, adiciona comandos OpenCode globais audit/UX/SPEC light/heavy e endurece workflows 9-18 para evidencia fisica. Marketplace.json agora aponta para `v7.7.0`. |
| v2.8 | 2026-05-24 | Bump 7.6.0 → 7.6.1 (PATCH — release hygiene closure). Aplica três correções de drift que foram detectadas durante a publicação do v7.6.0 no npm: (1) atualiza `Atualmente: **7.5.0**` → `Atualmente: **7.6.0**` na tabela canônica (estava defasado), promovendo agora para `7.6.1` neste bump; (2) sincroniza `.cursor-plugin/plugin.json` description com `.claude-plugin/plugin.json` description (estava defasado em duas minors); (3) bumpa `tests/regression/v7.1.0/F7_version_sync.cjs` constante VERSION pra 7.6.1 e PREV_VERSION pra 7.6.0 (estava 7.5.0/7.4.2). Suite passa de 55/3 falhas pra 58/1 — única falha remanescente é F2 langfuse vendoring (pre-existente desde v7.4.2 baseline, aceita e documentada como conhecida). Iron Law preservada: ZERO mudanças em agents/, skills/, references/, commands/, hooks/, lib/. Marketplace.json também bumpa `version: 7.6.1` + `ref: v7.6.1`. NPM publish da v7.6.1 acontece nesta mesma janela. Sessão de drafting da spec independente do projeto Paperclip-nativo (pipeline-orchestrator-paperclip) aconteceu em paralelo neste mesmo dia mas vive em pasta separada (D:/Pipeline Orchestrator Claude/paperclip-native-spec/) e NÃO faz parte deste release nem do plugin original. |
| v2.7 | 2026-05-22 | Bump 7.5.0 → 7.6.0 (MINOR — Paperclip integration layer + User Score Collection). Paperclip layer: nova subárvore `references/paperclip/` com 14 documentos (PAPERCLIP-AXIOMS + 6 workflow specs prompt-native + paperclip-kb + paperclip-catalog dos 46 agentes + paperclip-adaptation-spec + ZONING + PLAN-46-AGENTS-UPDATE + pilot-information-gate + install-junctions.bat) + 11 skills custom (engineering-principles canônica + 10 pipeline-orchestrator-* wrappers); novo slash command `commands/setup-paperclip.md` (orquestra detecção + install + configuração end-to-end via mklink /J no Windows); nova doc overview `docs/PAPERCLIP-INTEGRATION.md` (arquitetura + setup + gaps conhecidos). Permite rodar o plugin dentro do Paperclip (paperclip.ing — outro orquestrador open-source) via adapter `claude_local` com paridade efetiva ao Claude Code standalone. PARITY_VERDICT=PASS_WITH_NOTES validado em pilot E2E (leu agent.md original, GATE_REQUEST v1 schema confirmado, 22 skills detectadas, 4 references auxiliares lidas). User Score Collection promovida de [Unreleased] para [7.6.0] (`lib/execution-summary.cjs` + `lib/score-writer.cjs` + agents/core/final-validator.md Step 3.5). Iron Law preservada: ZERO mudanças em `agents/`, `skills/`, `references/` originais, `commands/` originais ou `lib/`. 23-row Mandatory Gates table e 35-row Registry untouched. Backward compat 100%. Known gaps P1 não bloqueantes: Opus 4.7 1M não reconhecido pelo Paperclip adapter (fallback Sonnet 4.6 OK); SessionStart:resume hooks warning 8s; `.claude/hooks/*.cjs` não executam em modo Paperclip (Paperclip orquestra lifecycle próprio). NPM publish destravado em 2026-05-24 com granular token + Bypass 2FA marcado (`npm publish --ignore-scripts` necessário para pular prepublishOnly com 1 falha pré-existente F2 langfuse vendoring; F7 + F1 corrigidas neste bump). |
| v2.6 | 2026-05-22 | Bump 7.4.2 → 7.5.0 (MINOR — concurrent-safe tracing). Implementa spec `.kiro/specs/tracing-concurrent-execution-id/` fechando 5 findings (A run-directory race, B-trace + B-span carrier keying, C state discovery, E tracing scope). Mudanças nucleares: (1) `lib/run-directory.cjs::allocate` agora cria diretório com `{recursive: false}` exclusivo + retry com `uniqueId` fresh em EEXIST (6 tentativas, espelho de `lib/run-log.cjs`); runId vira `${ordinal}-${uniqueId}-${slug}` (uniqueId = `Date.now().toString(36) + crypto.randomBytes(6).hex`); publica em `process.env.PIPELINE_RUN_ID`. (2) `lib/langfuse-carrier.cjs` API muda para `(runId, ppidFallback)` em getTracePath/getSpanPath/readTraceCarrier/writeTraceCarrier/cleanup\*; fallback PPID emite `CARRIER_PPID_FALLBACK` (deduped per process). (3) `.claude/hooks/sentinel-hook.cjs::discoverStatePath` e `.claude/hooks/stop-hook.cjs::findActiveRunFolder` adotam precedência `PIPELINE_DOC_PATH > PIPELINE_RUN_ID > mtime` com AUDIT `DISCOVERY_RUN_ID_NO_MATCH` / `DISCOVERY_MTIME_FALLBACK`. Sentinel-hook ganha `module.exports` + `require.main === module` guard. (4) `.claude/hooks/langfuse-hook.cjs` ganha `shouldTrace(toolName, scope)` puro + leitura de `PIPELINE_TRACING_SCOPE` (agent-only default = v7.4.0 parity / agent-plus-skill / full); unknown values cai em safe-default + AUDIT `TRACING_SCOPE_UNKNOWN`. (5) `lib/codex-operational-runtime.cjs` writers stampam `run_id` no `sentinel-state.json`. Codex mirror (`.codex/hooks/langfuse-hook.cjs` + `.codex/hooks/stop-hook.cjs`) sincronizado byte-identical. 5 regression tests novos em `tests/regression/v7.5.0/` (F1 run-directory race, F2/F3 carriers, F4 state discovery, F5 scope policy — 44 sub-assertions 100% GREEN). `tests/unit/run-directory.test.js` e `tests/unit/domain-integration.test.js` atualizados para o novo formato de runId (`external_consumers: none` per spec.json). `references/audit-trail.md` ganha "Concurrency Invariants", "Tracing Scope Policy" e catálogo de 6 AUDIT events; `references/sentinel-integration.md` ganha "State Discovery Contract". Backward compat preservada: default behavior bit-by-bit identical to v7.4.0; legacy single-arg PPID callers do carrier API ainda funcionam. Suite 43/48 GREEN (5 failures pré-existentes em v7.4.2 baseline). Iron Law respeitado; nenhum gate adicionado ao 22-row Mandatory table; nenhuma mudança em commands/skills/agents — só lib/, hooks/, references/, tests/. |
| v3.4 | 2026-06-12 | Bump 7.13.0 → 7.14.0 (MINOR — STEP 1.7 brainstorm enforcement determinístico). Dogfood run completo do próprio pipeline (Feature/COMPLEXA/feature-heavy, B0-B5, TDD RED→GREEN 26 cenários, 6 revisões adversariais per-batch + 3 finais, 27 achados fechados, Pa de Cal GO @ 0.85). Novo lib/step-1-7-routing.cjs (vocabulário fechado 4 branches + writers sanitizados) + references/step-1-7-enforcement.json + handleBrainstormDispatch no dispatch-guard (BRAINSTORM_BYPASS warn-first, deny via PIPELINE_BRAINSTORM_ENFORCEMENT, HMAC two-tier) + BRAINSTORM_EVIDENCE_MISSING no final-validator + warning no fidelity-reporter. Prova viva nos dois sentidos (fluxo correto 1ª tentativa + bypass deliberado detectado). Iron Law preservada; lockstep 7 arquivos; release nos canais GitHub/marketplace/npm + cache local. |
| v3.3 | 2026-06-09 | Bump 7.9.4 → 7.10.0 (MINOR — Plan Mode generalized + parallel dispatch). Plan Mode obrigatório expandido de 1 para 10 agentes (7 pesquisa + 2 implementadores + plan-architect); enforcement PLAN_MODE_BYPASS generalizado no pipeline-controller para a tabela inteira; despacho paralelo em lotes MEDIA com `parallel_eligible` flag (file-scope disjointness check via CHANGE_CONTRACT); `per_task_status` condicional no checkpoint-validator para atribuição por tarefa; revisão adversarial com 6 fixes (ADV-001/002, COR-001-005, ADV-006); 4 novos test files F20-F23 (74 cenários); F5 baseline range ajustado. Iron Law preservada; lockstep 7 arquivos; release nos canais GitHub/marketplace/npm + cache local. |
| v3.2 | 2026-06-06 | Bump 7.9.3 → 7.9.4 (PATCH — plan-mode contract fix). Dogfood run completo do próprio pipeline (Bug Fix/MEDIA/bugfix-light): corpo do plan-architect alinhado ao protocolo Achado #7 (Steps 0/3/4 + rodapé + 4 headers; Rule-1 label preservado p/ D7-S7), enforcement PLAN_MODE_BYPASS no pipeline-controller (evento AUDIT em protocol-events.jsonl + 1 re-dispatch, sem gate novo), testes F18 (fence-aware) + F19 pinando o contrato. Final adversarial 0 critical / 5 important (2 corrigidos em rework único, 3 follow-up); regressão D7-S7 detectada por verificação direta e reconciliada; Pa de Cal GO @ 0.94; suíte 62/67 = baseline. Release lockstep 7 arquivos; canais GitHub/marketplace/npm. |
| v3.1 | 2026-06-05 | Bump 7.9.2 → 7.9.3 (PATCH — telemetry recording fixes). Run dogfood do próprio pipeline (Bug Fix/MEDIA/bugfix-light, 3 batches, TDD 21 cenários RED→GREEN, adversarial per-batch nos B1-B2, Pa de Cal GO): B1 dedup do run-log (`shouldAppendRunLogEntry` no stop-hook, F15 7/7); B2 fidelity-report idempotente no teardown (`ensureFidelityReport` + `readClassification`, F16 6/6); B3 enriquecimento Langfuse (`readSentinelData` com precedência DOC_PATH>RUN_ID>mtime, `buildTraceMetadata` com run_id/type/complexity sanitizados, `resolveSpanName` anti-unknown, carrier tier doc-path, F17 8/8). Inclui F14 in-flight (Langfuse project isolation). Espelhos codex sha256-idênticos; Iron Law preservada (gates.md intocado); suíte-alvo verde com baseline in-flight provado alheio (arbitragem git-stash A/B). Lockstep 7 arquivos; release nos canais GitHub/marketplace/npm + cache local. |
| v3.0 | 2026-06-02 | Bump 7.9.1 → 7.9.2 (PATCH — documentation refresh, docs-only). Atualiza a documentação de usuário e referência para refletir as features acumuladas até v7.9.1: README + diagramas saem dos números antigos (v7.4.0 / v6.0.0) para v7.9.2, incluindo a correção da licença (MIT → PolyForm Shield 1.0.0) nos diagramas; documentação completa da camada Paperclip (integration layer v7.6.0 + company provisioner v7.8.0 + flow-mirror v7.9.0 com árvore de issues, 8 comandos `paperclip-*`, flag `--on=paperclip` e novo `references/paperclip/PAPERCLIP-FLOW-MIRROR.md`); seção run-log do audit-trail atualizada para o modelo v7.9.1 (buildRunLogEntry derivando total_gates_expected / fidelity_score / final_decision); vocabulário de decisão de gate alinhado aos 8 valores canônicos; e correção de drift de contagens (gate registry header 22 → 35, Mandatory table 22 → 23, suíte 61). Iron Law preservada: ZERO mudanças em código de runtime (agents/, skills/, references/ originais lógicos, commands/ originais, hooks/, lib/, tests/ exceto o pino de versão F7). Marketplace.json bumpa `version: 7.9.2` + `ref: v7.9.2`; F7 version-sync atualizado para 7.9.2 / PREV 7.9.1. |
